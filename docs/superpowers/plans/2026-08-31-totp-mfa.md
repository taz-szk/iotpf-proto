# TOTP MFA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** プラットフォーム管理者・テナントユーザー双方に TOTP 二要素認証を追加し、グローバル ON/OFF をプラットフォーム管理者が制御できるようにする。

**Architecture:** partial_token（type: partial_platform / partial_tenant、有効期限10分）をパスワード検証成功後に発行し、TOTP 検証通過後に通常 JWT/Cookie へ交換する。MFA 必須設定は `mfa_settings` テーブル1行で管理し、起動時マイグレーションで既存テナントスキーマへ列を自動追加する。

**Tech Stack:** pyotp>=2.9.0、qrcode.js (CDN)、既存 JWT インフラ (PyJWT)、FastAPI、Alpine.js

**Spec:** `docs/superpowers/specs/2026-08-31-totp-mfa-design.md`

## Global Constraints

- Python: 3.12。FastAPI 依存注入パターンは既存 `_bearer = HTTPBearer()` + Depends を踏襲する。
- partial_token の `type` フィールド値: `"partial_platform"` / `"partial_tenant"`（文字列完全一致）。
- `valid_window=1`（pyotp）: 前後 30 秒のウィンドウでコードを受け入れる。
- `totp_secret` 長: `pyotp.random_base32()` の出力（32文字 base32）を VARCHAR(64) に保存。
- `mfa_settings` テーブルは常に `id=1` の1行のみ。初期値は両方 `FALSE`。
- UI フレームワーク: Tailwind CSS + Alpine.js（既存ページと同一構成）。
- qrcode.js CDN: `https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js`
- 起動時マイグレーション関数名: `migrate_totp_columns`（database.py に追加）。
- 新規ルーター登録後は main.py の `on_startup` にも関数を追加すること。

---

### Task 1: TOTP 基盤（サービス・モデル・DB マイグレーション）

**Files:**
- Create: `core-api/app/services/totp.py`
- Modify: `core-api/app/models/public.py`
- Modify: `core-api/app/database.py`
- Modify: `core-api/requirements.txt`
- Modify: `postgres/init/01_schema.sql`

**Interfaces:**
- Produces:
  - `generate_totp_secret() -> str` — 32文字 base32 シークレット生成
  - `get_totp_uri(secret: str, email: str) -> str` — `otpauth://totp/...` URI
  - `verify_totp_code(secret: str, code: str) -> bool` — コード検証（valid_window=1）
  - `MfaSettings` SQLAlchemy モデル（`id`, `platform_required`, `tenant_required`）
  - `migrate_totp_columns()` — database.py に追加

- [ ] **Step 1: pyotp を requirements.txt に追加**

`core-api/requirements.txt` の末尾に追加:
```
pyotp>=2.9.0
```

- [ ] **Step 2: totp.py を作成**

`core-api/app/services/totp.py`:
```python
import pyotp

def generate_totp_secret() -> str:
    return pyotp.random_base32()

def get_totp_uri(secret: str, email: str) -> str:
    return pyotp.TOTP(secret).provisioning_uri(name=email, issuer_name="IoTAir-X")

def verify_totp_code(secret: str, code: str) -> bool:
    return pyotp.TOTP(secret).verify(code, valid_window=1)
```

- [ ] **Step 3: テストを書いて実行（失敗を確認）**

`core-api/tests/test_totp.py`:
```python
from app.services.totp import generate_totp_secret, get_totp_uri, verify_totp_code
import pyotp

def test_generate_secret_is_base32():
    secret = generate_totp_secret()
    assert len(secret) == 32
    assert secret.isalpha() or secret.replace("=", "").isalnum()

def test_get_totp_uri_format():
    secret = generate_totp_secret()
    uri = get_totp_uri(secret, "test@example.com")
    assert uri.startswith("otpauth://totp/")
    assert "IoTAir-X" in uri

def test_verify_totp_code_valid():
    secret = generate_totp_secret()
    code = pyotp.TOTP(secret).now()
    assert verify_totp_code(secret, code) is True

def test_verify_totp_code_invalid():
    secret = generate_totp_secret()
    assert verify_totp_code(secret, "000000") is False
```

```bash
cd core-api && pip install pyotp && pytest tests/test_totp.py -v
```

Expected: FAIL（モジュール未作成のため）→ Step 2 作成後: PASS

- [ ] **Step 4: MfaSettings モデルを models/public.py に追加**

`core-api/app/models/public.py` の末尾に追記:
```python
class MfaSettings(Base):
    __tablename__ = "mfa_settings"
    id = Column(Integer, primary_key=True, default=1)
    platform_required = Column(Boolean, nullable=False, default=False)
    tenant_required = Column(Boolean, nullable=False, default=False)
```

また `PlatformUser` クラスに2列を追加:
```python
class PlatformUser(Base):
    __tablename__ = "platform_users"
    id = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    email = Column(String(255), nullable=False, unique=True)
    password_hash = Column(String, nullable=False)
    is_active = Column(Boolean, nullable=False, default=True)
    token_version = Column(Integer, nullable=False, default=1)
    totp_secret = Column(String(64), nullable=True)          # 追加
    totp_enabled = Column(Boolean, nullable=False, default=False)  # 追加
    created_at = Column(DateTime(timezone=True), server_default=func.now())
```

- [ ] **Step 5: database.py に migrate_totp_columns を追加**

`core-api/app/database.py` の末尾に追記（既存 `migrate_add_device_name` の後）:
```python
def migrate_totp_columns() -> None:
    """platform_users と全テナント users テーブルに TOTP 列を追加（べき等）。"""
    with engine.connect() as conn:
        conn.execute(text("""
            ALTER TABLE platform_users
            ADD COLUMN IF NOT EXISTS totp_secret  VARCHAR(64),
            ADD COLUMN IF NOT EXISTS totp_enabled BOOLEAN NOT NULL DEFAULT FALSE
        """))
        conn.execute(text("""
            CREATE TABLE IF NOT EXISTS mfa_settings (
                id                INTEGER PRIMARY KEY DEFAULT 1,
                platform_required BOOLEAN NOT NULL DEFAULT FALSE,
                tenant_required   BOOLEAN NOT NULL DEFAULT FALSE,
                CHECK (id = 1)
            )
        """))
        conn.execute(text("""
            INSERT INTO mfa_settings (id, platform_required, tenant_required)
            VALUES (1, FALSE, FALSE)
            ON CONFLICT (id) DO NOTHING
        """))
        rows = conn.execute(text("SELECT id FROM tenants WHERE status != 'deleted'")).fetchall()
        conn.commit()
    for row in rows:
        schema = f"tenant_{str(row.id).replace('-', '_')}"
        with engine.connect() as conn:
            conn.execute(text(f"""
                ALTER TABLE "{schema}".users
                ADD COLUMN IF NOT EXISTS totp_secret  VARCHAR(64),
                ADD COLUMN IF NOT EXISTS totp_enabled BOOLEAN NOT NULL DEFAULT FALSE
            """))
            conn.commit()
```

- [ ] **Step 6: create_tenant_schema に TOTP 列を追加**

`database.py` の `create_tenant_schema` 内、`users` テーブル CREATE 文の `is_active` 行の後に追記:
```python
# users テーブル定義に以下を追加（is_active の下）
                is_active BOOLEAN NOT NULL DEFAULT TRUE,
                totp_secret VARCHAR(64),
                totp_enabled BOOLEAN NOT NULL DEFAULT FALSE,
                created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
```

- [ ] **Step 7: postgres/init/01_schema.sql に TOTP 関連を追加**

`postgres/init/01_schema.sql` の `platform_users` テーブル定義に列を追加:
```sql
CREATE TABLE IF NOT EXISTS platform_users (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    email VARCHAR(255) NOT NULL UNIQUE,
    password_hash TEXT NOT NULL,
    is_active BOOLEAN NOT NULL DEFAULT TRUE,
    token_version INTEGER NOT NULL DEFAULT 1,
    totp_secret VARCHAR(64),
    totp_enabled BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

同ファイルの末尾に `mfa_settings` テーブルを追加:
```sql
-- MFA グローバル設定（常に1行）
CREATE TABLE IF NOT EXISTS mfa_settings (
    id                INTEGER PRIMARY KEY DEFAULT 1,
    platform_required BOOLEAN NOT NULL DEFAULT FALSE,
    tenant_required   BOOLEAN NOT NULL DEFAULT FALSE,
    CHECK (id = 1)
);
INSERT INTO mfa_settings (id, platform_required, tenant_required)
VALUES (1, FALSE, FALSE)
ON CONFLICT (id) DO NOTHING;
```

- [ ] **Step 8: テストを再実行して確認**

```bash
cd core-api && pytest tests/test_totp.py -v
```
Expected: 4 tests PASS

- [ ] **Step 9: コミット**

```bash
git add core-api/app/services/totp.py core-api/app/models/public.py \
        core-api/app/database.py core-api/requirements.txt \
        postgres/init/01_schema.sql core-api/tests/test_totp.py
git commit -m "feat: TOTP 基盤追加（サービス・モデル・DBマイグレーション）"
```

---

### Task 2: プラットフォーム MFA API（auth.py 拡張 + mfa.py）

**Files:**
- Modify: `core-api/app/routers/auth.py`
- Modify: `core-api/app/schemas/auth.py`
- Create: `core-api/app/routers/mfa.py`

**Interfaces:**
- Consumes:
  - `generate_totp_secret()`, `get_totp_uri()`, `verify_totp_code()` from `app.services.totp`
  - `create_access_token(data, expires_delta)` from `app.services.auth`
  - `MfaSettings`, `PlatformUser` from `app.models.public`
- Produces:
  - `POST /auth/login` — レスポンスに `status`, `partial_token` フィールドを追加
  - `GET /auth/totp/setup` — `{otpauth_uri: str, secret: str}`
  - `POST /auth/totp/activate` — `{access_token, refresh_token, token_type}`
  - `POST /auth/totp/verify` — `{access_token, refresh_token, token_type}`
  - `DELETE /auth/totp` — 204

- [ ] **Step 1: schemas/auth.py に LoginOut を追加**

`core-api/app/schemas/auth.py` を以下に置き換え:
```python
from pydantic import BaseModel

class LoginRequest(BaseModel):
    email: str
    password: str

class TokenOut(BaseModel):
    access_token: str
    refresh_token: str
    token_type: str = "bearer"

class LoginOut(BaseModel):
    status: str = "ok"           # "ok" | "totp_required" | "totp_setup_required"
    access_token: str | None = None
    refresh_token: str | None = None
    token_type: str = "bearer"
    partial_token: str | None = None

class RefreshRequest(BaseModel):
    refresh_token: str
```

- [ ] **Step 2: auth.py の login エンドポイントを拡張**

`core-api/app/routers/auth.py` の `login` 関数を変更する。

import に追加:
```python
from datetime import timedelta
from app.models.public import MfaSettings
```

`login` 関数の `clear_failures(rate_key)` の直後（payload 構築の前）に MFA チェックを挿入:

```python
    clear_failures(rate_key)

    # MFA 設定確認
    with SessionLocal() as db:
        mfa = db.query(MfaSettings).filter(MfaSettings.id == 1).first()
    mfa_required = mfa.platform_required if mfa else False

    if mfa_required:
        partial_payload = {
            "sub": str(user.id),
            "email": user.email,
            "type": "partial_platform",
        }
        partial_token = create_access_token(partial_payload, expires_delta=timedelta(minutes=10))
        if user.totp_enabled:
            return LoginOut(status="totp_required", partial_token=partial_token)
        else:
            return LoginOut(status="totp_setup_required", partial_token=partial_token)
```

また `response_model=TokenOut` を `response_model=LoginOut` に変更し、末尾の `return TokenOut(...)` を:
```python
    return LoginOut(
        status="ok",
        access_token=access,
        refresh_token=refresh,
    )
```
に変更する。

- [ ] **Step 3: mfa.py を作成**

`core-api/app/routers/mfa.py`:
```python
from datetime import timedelta
from fastapi import APIRouter, Depends, HTTPException, Response, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from pydantic import BaseModel
from app.services.auth import verify_token, create_access_token, create_refresh_token, verify_password
from app.services.totp import generate_totp_secret, get_totp_uri, verify_totp_code
from app.models.public import PlatformUser
from app.database import SessionLocal

router = APIRouter(prefix="/auth/totp", tags=["mfa"])
_bearer = HTTPBearer()


def _require_partial_platform(creds: HTTPAuthorizationCredentials = Depends(_bearer)) -> dict:
    payload = verify_token(creds.credentials)
    if not payload or payload.get("type") != "partial_platform" or payload.get("token_type") != "access":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Unauthorized")
    return payload


def _full_payload(user: PlatformUser) -> dict:
    return {
        "sub": str(user.id),
        "email": user.email,
        "type": "platform",
        "tok_ver": user.token_version,
    }


class TotpCode(BaseModel):
    code: str

class DisableTotpRequest(BaseModel):
    password: str


@router.get("/setup")
def totp_setup(payload: dict = Depends(_require_partial_platform)):
    with SessionLocal() as db:
        user = db.query(PlatformUser).filter(PlatformUser.id == payload["sub"]).first()
        if not user:
            raise HTTPException(status_code=404, detail="User not found")
        if user.totp_enabled:
            raise HTTPException(status_code=404, detail="TOTP already active")
        if not user.totp_secret:
            user.totp_secret = generate_totp_secret()
            db.commit()
            db.refresh(user)
        secret = user.totp_secret
    uri = get_totp_uri(secret, payload["email"])
    return {"otpauth_uri": uri, "secret": secret}


@router.post("/activate")
def totp_activate(body: TotpCode, payload: dict = Depends(_require_partial_platform)):
    with SessionLocal() as db:
        user = db.query(PlatformUser).filter(PlatformUser.id == payload["sub"]).first()
        if not user or not user.totp_secret:
            raise HTTPException(status_code=400, detail="TOTP not initialized")
        if not verify_totp_code(user.totp_secret, body.code):
            raise HTTPException(status_code=400, detail="Invalid TOTP code")
        user.totp_enabled = True
        db.commit()
        db.refresh(user)
        full = _full_payload(user)
    from app.services.grafana import ensure_platform_admin_in_grafana
    try:
        ensure_platform_admin_in_grafana(user.email)
    except Exception:
        pass
    return {"access_token": create_access_token(full), "refresh_token": create_refresh_token(full), "token_type": "bearer"}


@router.post("/verify")
def totp_verify(body: TotpCode, payload: dict = Depends(_require_partial_platform)):
    with SessionLocal() as db:
        user = db.query(PlatformUser).filter(PlatformUser.id == payload["sub"]).first()
        if not user or not user.totp_enabled or not user.totp_secret:
            raise HTTPException(status_code=400, detail="TOTP not configured")
        if not verify_totp_code(user.totp_secret, body.code):
            raise HTTPException(status_code=400, detail="Invalid TOTP code")
        full = _full_payload(user)
    from app.services.grafana import ensure_platform_admin_in_grafana
    try:
        ensure_platform_admin_in_grafana(user.email)
    except Exception:
        pass
    return {"access_token": create_access_token(full), "refresh_token": create_refresh_token(full), "token_type": "bearer"}


@router.delete("", status_code=status.HTTP_204_NO_CONTENT)
def totp_disable(body: DisableTotpRequest, creds: HTTPAuthorizationCredentials = Depends(_bearer)):
    payload = verify_token(creds.credentials)
    if not payload or payload.get("type") != "platform" or payload.get("token_type") == "refresh":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Unauthorized")
    with SessionLocal() as db:
        user = db.query(PlatformUser).filter(PlatformUser.id == payload["sub"]).first()
        if not user or not verify_password(body.password, user.password_hash):
            raise HTTPException(status_code=400, detail="Invalid password")
        user.totp_enabled = False
        user.totp_secret = None
        user.token_version = user.token_version + 1
        db.commit()
```

- [ ] **Step 4: テストを書いて実行**

`core-api/tests/test_mfa_platform.py`:
```python
from unittest.mock import patch, MagicMock
from app.services.totp import generate_totp_secret, verify_totp_code
import pyotp

def test_totp_verify_roundtrip():
    secret = generate_totp_secret()
    code = pyotp.TOTP(secret).now()
    assert verify_totp_code(secret, code) is True

def test_totp_verify_wrong_code():
    secret = generate_totp_secret()
    assert verify_totp_code(secret, "000000") is False
```

```bash
cd core-api && pytest tests/test_mfa_platform.py tests/test_totp.py -v
```
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add core-api/app/routers/auth.py core-api/app/schemas/auth.py \
        core-api/app/routers/mfa.py core-api/tests/test_mfa_platform.py
git commit -m "feat: プラットフォーム TOTP API 追加（login拡張 + /auth/totp/*）"
```

---

### Task 3: テナント MFA API（tenant_auth.py 拡張 + tenant_mfa.py）

**Files:**
- Modify: `core-api/app/routers/tenant_auth.py`
- Create: `core-api/app/routers/tenant_mfa.py`

**Interfaces:**
- Consumes: Task 1 の `generate_totp_secret`, `get_totp_uri`, `verify_totp_code`、Task 2 の partial_token パターン
- Produces:
  - `POST /tenant-auth/login` — `status`, `partial_token` フィールドを追加
  - `GET /tenant-auth/totp/setup` — `{otpauth_uri, secret}`
  - `POST /tenant-auth/totp/activate` — Cookie を set して TenantLoginResponse を返す
  - `POST /tenant-auth/totp/verify` — Cookie を set して TenantLoginResponse を返す
  - `DELETE /tenant-auth/totp` — 204

- [ ] **Step 1: tenant_auth.py のログインレスポンスを拡張**

`core-api/app/schemas/tenant_auth.py` を確認し、`TenantLoginResponse` に `status` と `partial_token` を追加する。ファイルが存在しない場合は以下を作成:

```python
from pydantic import BaseModel

class TenantLoginRequest(BaseModel):
    tenant_slug: str
    email: str
    password: str

class TenantLoginResponse(BaseModel):
    status: str = "ok"              # "ok" | "totp_required" | "totp_setup_required"
    user_id: str | None = None
    email: str | None = None
    role: str | None = None
    tenant_id: str | None = None
    redirect_url: str | None = None
    partial_token: str | None = None
```

- [ ] **Step 2: tenant_auth.py の login に MFA チェックを追加**

`core-api/app/routers/tenant_auth.py` の `clear_failures(rate_key)` 直後に挿入:

import に追加:
```python
from datetime import timedelta
from app.models.public import MfaSettings
```

`clear_failures(rate_key)` 直後（Grafana sync の前）:
```python
    clear_failures(rate_key)

    # MFA 設定確認
    with SessionLocal() as db:
        mfa = db.query(MfaSettings).filter(MfaSettings.id == 1).first()
    mfa_required = mfa.tenant_required if mfa else False

    totp_enabled = bool(row.totp_enabled) if hasattr(row, "totp_enabled") else False

    if mfa_required:
        partial_payload = {
            "sub": str(row.id),
            "email": row.email,
            "type": "partial_tenant",
            "tenant_id": str(tenant.id),
            "role": row.role,
        }
        partial_token = create_access_token(partial_payload, expires_delta=timedelta(minutes=10))
        if totp_enabled:
            return TenantLoginResponse(status="totp_required", partial_token=partial_token)
        else:
            return TenantLoginResponse(status="totp_setup_required", partial_token=partial_token)
```

既存の `return TenantLoginResponse(...)` を以下に変更:
```python
    return TenantLoginResponse(
        status="ok",
        user_id=str(row.id),
        email=row.email,
        role=row.role,
        tenant_id=str(tenant.id),
        redirect_url="/admin/tenant-portal.html",
    )
```

また SELECT クエリに `totp_enabled` 列を追加:
```python
        row = conn.execute(
            text(f'SELECT id, email, password_hash, role, totp_enabled FROM "{schema}".users WHERE email = :email AND is_active = TRUE'),
            {"email": req.email}
        ).fetchone()
```

- [ ] **Step 3: tenant_mfa.py を作成**

`core-api/app/routers/tenant_mfa.py`:
```python
from datetime import timedelta
from fastapi import APIRouter, Cookie, Depends, HTTPException, Response, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from pydantic import BaseModel
from sqlalchemy import text
from app.services.auth import verify_token, create_access_token, verify_password, hash_password
from app.services.totp import generate_totp_secret, get_totp_uri, verify_totp_code
from app.models.public import Tenant
from app.database import SessionLocal, engine
from app.config import settings

router = APIRouter(prefix="/tenant-auth/totp", tags=["tenant-mfa"])
_bearer = HTTPBearer()


def _require_partial_tenant(creds: HTTPAuthorizationCredentials = Depends(_bearer)) -> dict:
    payload = verify_token(creds.credentials)
    if not payload or payload.get("type") != "partial_tenant" or payload.get("token_type") != "access":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Unauthorized")
    return payload


def _schema(tenant_id: str) -> str:
    return f"tenant_{tenant_id.replace('-', '_')}"


def _set_tenant_cookie(response: Response, payload: dict) -> None:
    token = create_access_token(payload, expires_delta=timedelta(hours=settings.grafana_session_expire_hours))
    response.set_cookie(
        key="iot_token", value=token,
        httponly=True, secure=True, samesite="lax",
        max_age=settings.grafana_session_expire_hours * 3600, path="/",
    )


class TotpCode(BaseModel):
    code: str

class DisableTotpRequest(BaseModel):
    password: str


@router.get("/setup")
def tenant_totp_setup(payload: dict = Depends(_require_partial_tenant)):
    tenant_id = payload["tenant_id"]
    user_id = payload["sub"]
    schema = _schema(tenant_id)
    with engine.connect() as conn:
        row = conn.execute(
            text(f'SELECT totp_secret, totp_enabled FROM "{schema}".users WHERE id = :uid'),
            {"uid": user_id},
        ).fetchone()
    if not row:
        raise HTTPException(status_code=404, detail="User not found")
    if row.totp_enabled:
        raise HTTPException(status_code=404, detail="TOTP already active")
    secret = row.totp_secret
    if not secret:
        secret = generate_totp_secret()
        with engine.connect() as conn:
            conn.execute(
                text(f'UPDATE "{schema}".users SET totp_secret = :s WHERE id = :uid'),
                {"s": secret, "uid": user_id},
            )
            conn.commit()
    return {"otpauth_uri": get_totp_uri(secret, payload["email"]), "secret": secret}


@router.post("/activate")
def tenant_totp_activate(body: TotpCode, response: Response, payload: dict = Depends(_require_partial_tenant)):
    tenant_id = payload["tenant_id"]
    user_id = payload["sub"]
    schema = _schema(tenant_id)
    with engine.connect() as conn:
        row = conn.execute(
            text(f'SELECT totp_secret FROM "{schema}".users WHERE id = :uid'),
            {"uid": user_id},
        ).fetchone()
    if not row or not row.totp_secret:
        raise HTTPException(status_code=400, detail="TOTP not initialized")
    if not verify_totp_code(row.totp_secret, body.code):
        raise HTTPException(status_code=400, detail="Invalid TOTP code")
    with engine.connect() as conn:
        conn.execute(
            text(f'UPDATE "{schema}".users SET totp_enabled = TRUE WHERE id = :uid'),
            {"uid": user_id},
        )
        conn.commit()
    full_payload = {
        "sub": user_id,
        "email": payload["email"],
        "type": "tenant",
        "tenant_id": tenant_id,
        "role": payload["role"],
    }
    _set_tenant_cookie(response, full_payload)
    return {
        "status": "ok",
        "user_id": user_id,
        "email": payload["email"],
        "role": payload["role"],
        "tenant_id": tenant_id,
        "redirect_url": "/admin/tenant-portal.html",
    }


@router.post("/verify")
def tenant_totp_verify(body: TotpCode, response: Response, payload: dict = Depends(_require_partial_tenant)):
    tenant_id = payload["tenant_id"]
    user_id = payload["sub"]
    schema = _schema(tenant_id)
    with engine.connect() as conn:
        row = conn.execute(
            text(f'SELECT totp_secret, totp_enabled FROM "{schema}".users WHERE id = :uid'),
            {"uid": user_id},
        ).fetchone()
    if not row or not row.totp_enabled or not row.totp_secret:
        raise HTTPException(status_code=400, detail="TOTP not configured")
    if not verify_totp_code(row.totp_secret, body.code):
        raise HTTPException(status_code=400, detail="Invalid TOTP code")
    full_payload = {
        "sub": user_id,
        "email": payload["email"],
        "type": "tenant",
        "tenant_id": tenant_id,
        "role": payload["role"],
    }
    _set_tenant_cookie(response, full_payload)
    return {
        "status": "ok",
        "user_id": user_id,
        "email": payload["email"],
        "role": payload["role"],
        "tenant_id": tenant_id,
        "redirect_url": "/admin/tenant-portal.html",
    }


@router.delete("", status_code=status.HTTP_204_NO_CONTENT)
def tenant_totp_disable(body: DisableTotpRequest, iot_token: str = Cookie(default=None)):
    if not iot_token:
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Not authenticated")
    payload = verify_token(iot_token)
    if not payload or payload.get("type") != "tenant":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Not authenticated")
    tenant_id = payload["tenant_id"]
    user_id = payload["sub"]
    schema = _schema(tenant_id)
    with engine.connect() as conn:
        row = conn.execute(
            text(f'SELECT password_hash FROM "{schema}".users WHERE id = :uid'),
            {"uid": user_id},
        ).fetchone()
    if not row or not verify_password(body.password, row.password_hash):
        raise HTTPException(status_code=400, detail="Invalid password")
    with engine.connect() as conn:
        conn.execute(
            text(f'UPDATE "{schema}".users SET totp_enabled = FALSE, totp_secret = NULL WHERE id = :uid'),
            {"uid": user_id},
        )
        conn.commit()
```

- [ ] **Step 4: コミット**

```bash
git add core-api/app/routers/tenant_auth.py \
        core-api/app/routers/tenant_mfa.py
git commit -m "feat: テナント TOTP API 追加（tenant-auth login拡張 + /tenant-auth/totp/*）"
```

---

### Task 4: MFA グローバル設定 API + main.py 登録

**Files:**
- Create: `core-api/app/routers/platform.py`
- Modify: `core-api/app/main.py`

**Interfaces:**
- Consumes: `MfaSettings` モデル（Task 1）、`_bearer` + `verify_token` パターン（既存 auth.py）
- Produces:
  - `GET /platform/mfa-settings` → `{platform_required: bool, tenant_required: bool}`
  - `PATCH /platform/mfa-settings` → 同上（更新後）
  - main.py に `migrate_totp_columns` + 新ルーター登録

- [ ] **Step 1: platform.py を作成**

`core-api/app/routers/platform.py`:
```python
from fastapi import APIRouter, Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from pydantic import BaseModel
from app.models.public import MfaSettings
from app.services.auth import verify_token
from app.database import SessionLocal

router = APIRouter(prefix="/platform", tags=["platform"])
_bearer = HTTPBearer()


def _require_platform(creds: HTTPAuthorizationCredentials = Depends(_bearer)) -> dict:
    payload = verify_token(creds.credentials)
    if not payload or payload.get("type") != "platform" or payload.get("token_type") == "refresh":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Unauthorized")
    return payload


class MfaSettingsUpdate(BaseModel):
    platform_required: bool | None = None
    tenant_required: bool | None = None


@router.get("/mfa-settings")
def get_mfa_settings(_: dict = Depends(_require_platform)):
    with SessionLocal() as db:
        s = db.query(MfaSettings).filter(MfaSettings.id == 1).first()
        if not s:
            return {"platform_required": False, "tenant_required": False}
        return {"platform_required": s.platform_required, "tenant_required": s.tenant_required}


@router.patch("/mfa-settings")
def update_mfa_settings(body: MfaSettingsUpdate, _: dict = Depends(_require_platform)):
    with SessionLocal() as db:
        s = db.query(MfaSettings).filter(MfaSettings.id == 1).first()
        if not s:
            raise HTTPException(status_code=500, detail="MFA settings not initialized")
        if body.platform_required is not None:
            s.platform_required = body.platform_required
        if body.tenant_required is not None:
            s.tenant_required = body.tenant_required
        db.commit()
        db.refresh(s)
        return {"platform_required": s.platform_required, "tenant_required": s.tenant_required}
```

- [ ] **Step 2: main.py を更新**

`core-api/app/main.py` の import 行を更新:
```python
from app.routers import (
    health, auth, tenants, provisioning, emqx, provisioning_tokens,
    alert_rules, emqx_events, firmware, stats, tenant_auth, tenant_users,
    tenant_devices, tenant_grafana, tenant_portal, public_access, mfa, tenant_mfa, platform,
)
from app.database import (
    migrate_add_grafana_org_id, migrate_add_device_name,
    migrate_add_provisioning_token_id, migrate_add_public_token,
    migrate_add_token_version, migrate_totp_columns,
)
```

`on_startup` の migrate リストに `migrate_totp_columns` を追加:
```python
    for migrate in (
        migrate_add_grafana_org_id, migrate_add_device_name,
        migrate_add_provisioning_token_id, migrate_add_public_token,
        migrate_add_token_version, migrate_totp_columns,
    ):
```

`app.include_router` 群に3行追加:
```python
app.include_router(mfa.router)
app.include_router(tenant_mfa.router)
app.include_router(platform.router)
```

- [ ] **Step 3: コンテナ内で起動確認**

```bash
docker compose up -d --build core-api
docker compose logs core-api --tail=30
```
Expected: `Application startup complete` が表示され、エラーなし。

- [ ] **Step 4: コミット**

```bash
git add core-api/app/routers/platform.py core-api/app/main.py
git commit -m "feat: MFA グローバル設定 API 追加 + ルーター登録"
```

---

### Task 5: 管理者 UI（ログイン拡張 + TOTP ページ + 設定ページ）

**Files:**
- Modify: `admin-ui/index.html`
- Modify: `admin-ui/js/api.js`
- Create: `admin-ui/totp-verify.html`
- Create: `admin-ui/totp-setup.html`
- Create: `admin-ui/platform-settings.html`

**Interfaces:**
- Consumes: Task 2・4 の API エンドポイント
- Produces: 管理者ログイン完全 TOTP フロー、MFA 設定 UI

- [ ] **Step 1: api.js に TOTP・Platform エンドポイントを追加**

`admin-ui/js/api.js` の `api` オブジェクトに追記:
```javascript
    totp: {
        setup:    () => request('GET',    '/auth/totp/setup'),
        activate: (code) => request('POST', '/auth/totp/activate', { code }),
        verify:   (code) => request('POST', '/auth/totp/verify',   { code }),
        disable:  (password) => request('DELETE', '/auth/totp', { password }),
    },
    platform: {
        getMfaSettings: () => request('GET',   '/platform/mfa-settings'),
        updateMfaSettings: (body) => request('PATCH', '/platform/mfa-settings', body),
        grafanaOrgId: () => request('GET', '/tenants/platform/grafana-org-id'),
    },
```

※ 既存の `platform: { grafanaOrgId: ... }` は上記にマージして置き換える。

また `request` 関数の前に部分トークン用ヘルパーを追加:
```javascript
function getPartialToken() { return sessionStorage.getItem('iot_partial_token'); }
function setPartialToken(t) { sessionStorage.setItem('iot_partial_token', t); }
function clearPartialToken() { sessionStorage.removeItem('iot_partial_token'); }

async function partialRequest(method, path, body = null) {
    const token = getPartialToken();
    const headers = { 'Content-Type': 'application/json' };
    if (token) headers['Authorization'] = `Bearer ${token}`;
    const resp = await fetch(`${API_BASE}${path}`, {
        method, headers,
        body: body ? JSON.stringify(body) : null,
    });
    if (!resp.ok) {
        const err = await resp.json().catch(() => ({ detail: resp.statusText }));
        throw new Error(err.detail || 'Request failed');
    }
    if (resp.status === 204) return null;
    return resp.json();
}
```

`api` オブジェクトに `partialRequest` を使う totp エンドポイントを追記:
```javascript
    totpPartial: {
        setup:    () => partialRequest('GET',  '/auth/totp/setup'),
        activate: (code) => partialRequest('POST', '/auth/totp/activate', { code }),
        verify:   (code) => partialRequest('POST', '/auth/totp/verify',   { code }),
    },
```

また `window.api` の後に以下を追加:
```javascript
window.setPartialToken = setPartialToken;
window.clearPartialToken = clearPartialToken;
```

- [ ] **Step 2: index.html のログイン後処理を拡張**

`admin-ui/index.html` のログイン成功時の処理（`api.setTokens(...)` を呼んでいる箇所）を以下に変更:

```javascript
// 変更前（例）
api.setTokens(data.access_token, data.refresh_token);
window.location.href = '/admin/tenants.html';

// 変更後
if (data.status === 'totp_required') {
    setPartialToken(data.partial_token);
    window.location.href = '/admin/totp-verify.html';
} else if (data.status === 'totp_setup_required') {
    setPartialToken(data.partial_token);
    window.location.href = '/admin/totp-setup.html';
} else {
    api.setTokens(data.access_token, data.refresh_token);
    window.location.href = '/admin/tenants.html';
}
```

※ 実際の index.html のログイン処理箇所を確認して適用すること。

- [ ] **Step 3: totp-verify.html を作成**

`admin-ui/totp-verify.html`:
```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>2段階認証 - IoTAir-X</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/admin/js/api.js?v=11"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8 w-full max-w-sm">
    <h1 class="text-xl font-bold text-gray-800 mb-2">2段階認証</h1>
    <p class="text-sm text-gray-500 mb-6">Google Authenticator に表示されている 6 桁のコードを入力してください。</p>
    <div id="error" class="hidden mb-4 text-sm text-red-600 bg-red-50 border border-red-200 rounded px-3 py-2"></div>
    <form id="form">
      <input id="code" type="text" inputmode="numeric" pattern="[0-9]{6}" maxlength="6"
             placeholder="000000" required
             class="w-full border border-gray-300 rounded px-3 py-2 text-center text-2xl tracking-widest mb-4 focus:outline-none focus:ring-2 focus:ring-blue-500">
      <button type="submit"
              class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded">
        確認する
      </button>
    </form>
    <p class="mt-4 text-center text-sm">
      <a href="/admin/" class="text-gray-400 hover:text-gray-600">← ログイン画面に戻る</a>
    </p>
  </div>
  <script>
    if (!sessionStorage.getItem('iot_partial_token')) {
      window.location.href = '/admin/';
    }
    document.getElementById('form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const code = document.getElementById('code').value.trim();
      const errEl = document.getElementById('error');
      errEl.classList.add('hidden');
      try {
        const data = await api.totpPartial.verify(code);
        api.setTokens(data.access_token, data.refresh_token);
        clearPartialToken();
        window.location.href = '/admin/tenants.html';
      } catch (err) {
        errEl.textContent = err.message || '認証に失敗しました';
        errEl.classList.remove('hidden');
        document.getElementById('code').value = '';
      }
    });
  </script>
</body>
</html>
```

- [ ] **Step 4: totp-setup.html を作成**

`admin-ui/totp-setup.html`:
```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>2段階認証の設定 - IoTAir-X</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
  <script src="/admin/js/api.js?v=11"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8 w-full max-w-md">
    <h1 class="text-xl font-bold text-gray-800 mb-2">2段階認証を設定</h1>
    <p class="text-sm text-gray-500 mb-6">Google Authenticator などの認証アプリで QR コードをスキャンしてください。</p>
    <div id="error" class="hidden mb-4 text-sm text-red-600 bg-red-50 border border-red-200 rounded px-3 py-2"></div>
    <div id="loading" class="text-center py-8 text-gray-400">読み込み中...</div>
    <div id="content" class="hidden">
      <div id="qrcode" class="flex justify-center mb-4"></div>
      <details class="mb-4">
        <summary class="text-xs text-gray-400 cursor-pointer">QR が読めない場合はこちら（手動入力）</summary>
        <code id="secret" class="text-xs bg-gray-100 rounded px-2 py-1 mt-1 block break-all"></code>
      </details>
      <form id="form">
        <label class="block text-sm font-medium text-gray-700 mb-1">設定後に表示される 6 桁のコードを入力</label>
        <input id="code" type="text" inputmode="numeric" pattern="[0-9]{6}" maxlength="6"
               placeholder="000000" required
               class="w-full border border-gray-300 rounded px-3 py-2 text-center text-2xl tracking-widest mb-4 focus:outline-none focus:ring-2 focus:ring-blue-500">
        <button type="submit"
                class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded">
          設定を完了する
        </button>
      </form>
    </div>
  </div>
  <script>
    if (!sessionStorage.getItem('iot_partial_token')) {
      window.location.href = '/admin/';
    }
    (async () => {
      try {
        const data = await api.totpPartial.setup();
        document.getElementById('loading').classList.add('hidden');
        document.getElementById('content').classList.remove('hidden');
        new QRCode(document.getElementById('qrcode'), { text: data.otpauth_uri, width: 200, height: 200 });
        document.getElementById('secret').textContent = data.secret;
      } catch (err) {
        document.getElementById('loading').textContent = err.message || '読み込みに失敗しました';
      }
    })();
    document.getElementById('form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const code = document.getElementById('code').value.trim();
      const errEl = document.getElementById('error');
      errEl.classList.add('hidden');
      try {
        const data = await api.totpPartial.activate(code);
        api.setTokens(data.access_token, data.refresh_token);
        clearPartialToken();
        window.location.href = '/admin/tenants.html';
      } catch (err) {
        errEl.textContent = err.message || '設定に失敗しました';
        errEl.classList.remove('hidden');
        document.getElementById('code').value = '';
      }
    });
  </script>
</body>
</html>
```

- [ ] **Step 5: platform-settings.html を作成**

`admin-ui/platform-settings.html`:
```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>プラットフォーム設定 - IoTAir-X</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
  <script src="/admin/js/api.js?v=11"></script>
</head>
<body class="bg-gray-50 min-h-screen">
  <nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
    <div class="flex items-center gap-3">
      <a href="/admin/tenants.html" class="text-gray-400 hover:text-gray-600 text-sm">← テナント一覧</a>
      <span class="text-gray-300">|</span>
      <h1 class="text-lg font-bold text-gray-800">プラットフォーム設定</h1>
    </div>
    <div class="flex items-center gap-4">
      <a href="/admin/admin-change-password.html" class="text-sm text-gray-500 hover:text-gray-700">パスワード変更</a>
      <button onclick="api.logout()" class="text-sm text-gray-500 hover:text-gray-700">ログアウト</button>
    </div>
  </nav>
  <div x-data="settingsApp()" class="max-w-2xl mx-auto px-6 py-8">
    <div x-show="loading" class="text-center py-12 text-gray-400">読み込み中...</div>
    <div x-show="error" class="bg-red-50 border border-red-200 text-red-600 rounded px-4 py-3 mb-4" x-text="error"></div>
    <div x-show="!loading" class="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
      <h2 class="text-base font-semibold text-gray-800 mb-4">多要素認証（TOTP）設定</h2>
      <div class="space-y-4">
        <div class="flex items-center justify-between py-3 border-b border-gray-100">
          <div>
            <p class="text-sm font-medium text-gray-700">プラットフォーム管理者に TOTP を必須にする</p>
            <p class="text-xs text-gray-400 mt-0.5">有効にすると、管理者はログイン時に 6 桁コードの入力が必要になります</p>
          </div>
          <button @click="toggle('platform_required')"
                  :class="settings.platform_required ? 'bg-blue-600' : 'bg-gray-200'"
                  class="relative inline-flex h-6 w-11 items-center rounded-full transition-colors">
            <span :class="settings.platform_required ? 'translate-x-6' : 'translate-x-1'"
                  class="inline-block h-4 w-4 transform rounded-full bg-white transition-transform"></span>
          </button>
        </div>
        <div class="flex items-center justify-between py-3">
          <div>
            <p class="text-sm font-medium text-gray-700">テナントユーザーに TOTP を必須にする</p>
            <p class="text-xs text-gray-400 mt-0.5">有効にすると、全テナントのユーザーがログイン時に 6 桁コードの入力が必要になります</p>
          </div>
          <button @click="toggle('tenant_required')"
                  :class="settings.tenant_required ? 'bg-blue-600' : 'bg-gray-200'"
                  class="relative inline-flex h-6 w-11 items-center rounded-full transition-colors">
            <span :class="settings.tenant_required ? 'translate-x-6' : 'translate-x-1'"
                  class="inline-block h-4 w-4 transform rounded-full bg-white transition-transform"></span>
          </button>
        </div>
      </div>
      <p x-show="saved" class="mt-4 text-sm text-green-600">設定を保存しました</p>
    </div>
  </div>
  <script>
    if (!api.isLoggedIn()) window.location.href = '/admin/';
    function settingsApp() {
      return {
        loading: true, error: '', saved: false,
        settings: { platform_required: false, tenant_required: false },
        async init() {
          try {
            this.settings = await api.platform.getMfaSettings();
          } catch(e) { this.error = e.message; }
          finally { this.loading = false; }
        },
        async toggle(key) {
          this.saved = false;
          this.settings[key] = !this.settings[key];
          try {
            await api.platform.updateMfaSettings({ [key]: this.settings[key] });
            this.saved = true;
            setTimeout(() => this.saved = false, 3000);
          } catch(e) { this.error = e.message; this.settings[key] = !this.settings[key]; }
        },
      };
    }
  </script>
</body>
</html>
```

- [ ] **Step 6: tenants.html のナビバーに「設定」リンクを追加**

`admin-ui/tenants.html` のナビバー（ログアウトボタンの隣）に追加:
```html
<a href="/admin/platform-settings.html" class="text-sm text-gray-500 hover:text-gray-700">設定</a>
```

- [ ] **Step 7: コミット**

```bash
git add admin-ui/js/api.js admin-ui/index.html \
        admin-ui/totp-verify.html admin-ui/totp-setup.html \
        admin-ui/platform-settings.html admin-ui/tenants.html
git commit -m "feat: 管理者 TOTP UI 追加（ログイン拡張・verify/setup ページ・設定ページ）"
```

---

### Task 6: テナント UI（テナントログイン拡張 + TOTP ページ）

**Files:**
- Modify: `admin-ui/tenant-login.html`
- Create: `admin-ui/tenant-totp-verify.html`
- Create: `admin-ui/tenant-totp-setup.html`

**Interfaces:**
- Consumes: Task 3 の `/tenant-auth/totp/*` API（Bearer トークンで呼ぶ）
- Produces: テナントユーザーの完全 TOTP ログインフロー

- [ ] **Step 1: api.js にテナント TOTP エンドポイントを追加**

`admin-ui/js/api.js` の `api` オブジェクトに追記（Task 5 の変更に続けて）:
```javascript
    tenantTotpPartial: {
        setup:    () => partialRequest('GET',  '/tenant-auth/totp/setup'),
        activate: (code) => partialRequest('POST', '/tenant-auth/totp/activate', { code }),
        verify:   (code) => partialRequest('POST', '/tenant-auth/totp/verify',   { code }),
    },
```

- [ ] **Step 2: tenant-login.html のログイン後処理を拡張**

`admin-ui/tenant-login.html` のログイン成功後処理（`redirect_url` にリダイレクトしている箇所）を以下に変更:
```javascript
if (data.status === 'totp_required') {
    setPartialToken(data.partial_token);
    window.location.href = '/admin/tenant-totp-verify.html';
} else if (data.status === 'totp_setup_required') {
    setPartialToken(data.partial_token);
    window.location.href = '/admin/tenant-totp-setup.html';
} else {
    window.location.href = data.redirect_url || '/admin/tenant-portal.html';
}
```

- [ ] **Step 3: tenant-totp-verify.html を作成**

`admin-ui/tenant-totp-verify.html`:
```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>2段階認証 - テナントログイン</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="/admin/js/api.js?v=11"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8 w-full max-w-sm">
    <h1 class="text-xl font-bold text-gray-800 mb-2">2段階認証</h1>
    <p class="text-sm text-gray-500 mb-6">Google Authenticator に表示されている 6 桁のコードを入力してください。</p>
    <div id="error" class="hidden mb-4 text-sm text-red-600 bg-red-50 border border-red-200 rounded px-3 py-2"></div>
    <form id="form">
      <input id="code" type="text" inputmode="numeric" pattern="[0-9]{6}" maxlength="6"
             placeholder="000000" required
             class="w-full border border-gray-300 rounded px-3 py-2 text-center text-2xl tracking-widest mb-4 focus:outline-none focus:ring-2 focus:ring-blue-500">
      <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded">
        確認する
      </button>
    </form>
    <p class="mt-4 text-center text-sm">
      <a href="/admin/tenant-login.html" class="text-gray-400 hover:text-gray-600">← ログイン画面に戻る</a>
    </p>
  </div>
  <script>
    if (!sessionStorage.getItem('iot_partial_token')) {
      window.location.href = '/admin/tenant-login.html';
    }
    document.getElementById('form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const code = document.getElementById('code').value.trim();
      const errEl = document.getElementById('error');
      errEl.classList.add('hidden');
      try {
        const data = await api.tenantTotpPartial.verify(code);
        clearPartialToken();
        window.location.href = data.redirect_url || '/admin/tenant-portal.html';
      } catch (err) {
        errEl.textContent = err.message || '認証に失敗しました';
        errEl.classList.remove('hidden');
        document.getElementById('code').value = '';
      }
    });
  </script>
</body>
</html>
```

- [ ] **Step 4: tenant-totp-setup.html を作成**

`admin-ui/tenant-totp-setup.html`:
```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>2段階認証の設定 - テナントログイン</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script>
  <script src="/admin/js/api.js?v=11"></script>
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
  <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8 w-full max-w-md">
    <h1 class="text-xl font-bold text-gray-800 mb-2">2段階認証を設定</h1>
    <p class="text-sm text-gray-500 mb-6">Google Authenticator などの認証アプリで QR コードをスキャンしてください。</p>
    <div id="error" class="hidden mb-4 text-sm text-red-600 bg-red-50 border border-red-200 rounded px-3 py-2"></div>
    <div id="loading" class="text-center py-8 text-gray-400">読み込み中...</div>
    <div id="content" class="hidden">
      <div id="qrcode" class="flex justify-center mb-4"></div>
      <details class="mb-4">
        <summary class="text-xs text-gray-400 cursor-pointer">QR が読めない場合はこちら（手動入力）</summary>
        <code id="secret" class="text-xs bg-gray-100 rounded px-2 py-1 mt-1 block break-all"></code>
      </details>
      <form id="form">
        <label class="block text-sm font-medium text-gray-700 mb-1">設定後に表示される 6 桁のコードを入力</label>
        <input id="code" type="text" inputmode="numeric" pattern="[0-9]{6}" maxlength="6"
               placeholder="000000" required
               class="w-full border border-gray-300 rounded px-3 py-2 text-center text-2xl tracking-widest mb-4 focus:outline-none focus:ring-2 focus:ring-blue-500">
        <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded">
          設定を完了する
        </button>
      </form>
    </div>
  </div>
  <script>
    if (!sessionStorage.getItem('iot_partial_token')) {
      window.location.href = '/admin/tenant-login.html';
    }
    (async () => {
      try {
        const data = await api.tenantTotpPartial.setup();
        document.getElementById('loading').classList.add('hidden');
        document.getElementById('content').classList.remove('hidden');
        new QRCode(document.getElementById('qrcode'), { text: data.otpauth_uri, width: 200, height: 200 });
        document.getElementById('secret').textContent = data.secret;
      } catch (err) {
        document.getElementById('loading').textContent = err.message || '読み込みに失敗しました';
      }
    })();
    document.getElementById('form').addEventListener('submit', async (e) => {
      e.preventDefault();
      const code = document.getElementById('code').value.trim();
      const errEl = document.getElementById('error');
      errEl.classList.add('hidden');
      try {
        const data = await api.tenantTotpPartial.activate(code);
        clearPartialToken();
        window.location.href = data.redirect_url || '/admin/tenant-portal.html';
      } catch (err) {
        errEl.textContent = err.message || '設定に失敗しました';
        errEl.classList.remove('hidden');
        document.getElementById('code').value = '';
      }
    });
  </script>
</body>
</html>
```

- [ ] **Step 5: コミット**

```bash
git add admin-ui/js/api.js admin-ui/tenant-login.html \
        admin-ui/tenant-totp-verify.html admin-ui/tenant-totp-setup.html
git commit -m "feat: テナント TOTP UI 追加（ログイン拡張・verify/setup ページ）"
```

---

## 実装完了後の動作確認手順

```bash
# 1. core-api リビルド（マイグレーション自動実行）
docker compose up -d --build core-api
docker compose logs core-api --tail=20   # エラーなし確認

# 2. MFA 設定を ON にする（管理者でログイン後、設定ページから）
#    または API で直接:
curl -X PATCH https://localhost/api/platform/mfa-settings \
  -H "Authorization: Bearer <access_token>" \
  -H "Content-Type: application/json" \
  -d '{"platform_required": true}'

# 3. 管理者ログイン → totp-setup.html にリダイレクトされることを確認
# 4. QR コードをスキャン → コード入力 → tenants.html にリダイレクト
# 5. 再ログイン → totp-verify.html にリダイレクト → コード入力でログイン完了
```
