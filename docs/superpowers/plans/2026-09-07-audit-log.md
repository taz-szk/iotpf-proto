# 監査ログ機能 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** テナント単位・PF管理者単位で全ユーザー操作を記録し、管理者が画面で確認できる監査ログ機能を追加する。

**Architecture:** PostgreSQL の `public.audit_logs` テーブルに記録。ルーターの各エンドポイントで `write_audit_log()` を明示的に呼ぶ方式。保存期間は設定値（デフォルト365日）で管理し、バックグラウンドスレッドが毎日古いレコードを削除する。PF管理者は全テナントのログを参照でき、テナント管理者は自テナント分のみ参照できる。

**Tech Stack:** FastAPI / SQLAlchemy / PostgreSQL / Alpine.js / Tailwind CSS（既存スタック）

**Spec:** `docs/superpowers/specs/2026-09-07-audit-log-design.md`

## Global Constraints

- マイグレーションは `database.py` に `migrate_create_audit_logs()` を追加し `main.py` の `on_startup()` で呼ぶ（べき等、`CREATE TABLE IF NOT EXISTS`）
- 既存の認証パターン（`_require_platform` / `_require_admin`）を踏襲すること
- `write_audit_log()` は Session を受け取りcommitしない。呼び出し側が既存の `db.commit()` で一緒にコミットする
- セッションがないパス（認証失敗など）では `log_audit()` を使う。これは内部でSessionを作りcommitし、例外を全てcatchする
- 監査ログの失敗は本来の操作をブロックしてはならない（全 `log_audit()` 呼び出しは try/except 不要、`log_audit()` 内部で吸収）
- フロントエンド変更後は必ず `npm run build:css` を実行してからコミットする
- Python テストは `pytest core-api/tests/` で実行（`core-api/` ディレクトリで実行する場合は `pytest tests/`）

---

## ファイル構成

**新規作成:**
- `core-api/app/services/audit.py` — `write_audit_log()` + `log_audit()` + `start_audit_purge_worker()`
- `core-api/app/schemas/audit.py` — `AuditLogOut`, `AuditLogListOut`
- `core-api/app/routers/audit_logs.py` — `GET /audit-logs`（PF管理者）
- `core-api/tests/test_audit.py` — 監査サービスのテスト
- `admin-ui/audit-logs.html` — PF管理者向け監査ログ画面

**変更:**
- `core-api/app/models/public.py` — `AuditLog` モデル追加
- `core-api/app/config.py` — `audit_log_retention_days` 追加
- `core-api/app/database.py` — `migrate_create_audit_logs()` 追加
- `core-api/app/main.py` — マイグレーション登録・パージWorker起動・ルーター登録
- `core-api/app/routers/auth.py` — login/logout/change_password にログ追加
- `core-api/app/routers/tenant_auth.py` — login/logout/change_password にログ追加
- `core-api/app/routers/mfa.py` — totp_activate/disable にログ追加
- `core-api/app/routers/tenant_mfa.py` — tenant totp_activate/disable にログ追加
- `core-api/app/routers/platform.py` — mfa_settings_updated にログ追加
- `core-api/app/routers/tenants.py` — CRUD操作にログ追加（`_` → `payload`）
- `core-api/app/routers/tenant_users.py` — create/delete にログ追加（`_` → `payload`）
- `core-api/app/routers/tenant_devices.py` — delete にログ追加（`_` → `payload`）
- `core-api/app/routers/firmware.py` — upload/delete/OTA にログ追加（`_` → `payload`）
- `core-api/app/routers/provisioning_tokens.py` — create/delete にログ追加（`_` → `payload`）
- `core-api/app/routers/tenant_portal.py` — `GET /tenant-portal/audit-logs` + device/firmware/token操作にログ追加
- `admin-ui/tenant-portal.html` — 監査ログタブ追加

---

### Task 1: DBモデル・マイグレーション・監査サービス基盤

**Files:**
- Modify: `core-api/app/models/public.py`
- Modify: `core-api/app/config.py`
- Modify: `core-api/app/database.py`
- Create: `core-api/app/services/audit.py`
- Modify: `core-api/app/main.py`
- Test: `core-api/tests/test_audit.py`

**Interfaces:**
- Produces: `write_audit_log(db: Session, actor_type: str, actor_id: str, actor_email: str, action: str, *, tenant_id: str | None = None, resource_type: str | None = None, resource_id: str | None = None, detail: dict | None = None, ip_address: str | None = None, result: str = "success") -> None`
- Produces: `log_audit(actor_type: str, actor_id: str, actor_email: str, action: str, **kwargs) -> None` （内部でSessionを作りcommit・例外吸収）
- Produces: `start_audit_purge_worker() -> None`
- Produces: `AuditLog` SQLAlchemy モデル（`app.models.public` から import 可能）

- [ ] **Step 1: `AuditLog` モデルを `app/models/public.py` に追加する**

`core-api/app/models/public.py` の末尾に追加：

```python
from sqlalchemy.dialects.postgresql import JSONB

class AuditLog(Base):
    __tablename__ = "audit_logs"
    id            = Column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    actor_type    = Column(String(20),  nullable=False)
    actor_id      = Column(UUID(as_uuid=True), nullable=False)
    actor_email   = Column(String(255), nullable=False)
    tenant_id     = Column(UUID(as_uuid=True), ForeignKey("tenants.id", ondelete="SET NULL"), nullable=True)
    action        = Column(String(100), nullable=False)
    resource_type = Column(String(50),  nullable=True)
    resource_id   = Column(String(255), nullable=True)
    detail        = Column(JSONB,       nullable=True)
    ip_address    = Column(String(45),  nullable=True)
    result        = Column(String(10),  nullable=False, default="success")
    created_at    = Column(DateTime(timezone=True), server_default=func.now(), nullable=False)
```

- [ ] **Step 2: `config.py` に `audit_log_retention_days` を追加する**

`core-api/app/config.py` の `Settings` クラス内（`grafana_session_expire_hours` の次の行）に追加：

```python
    audit_log_retention_days: int = 365
    # 環境変数 AUDIT_LOG_RETENTION_DAYS で上書き可
```

- [ ] **Step 3: `database.py` に `migrate_create_audit_logs()` を追加する**

`core-api/app/database.py` の末尾に追加：

```python
def migrate_create_audit_logs() -> None:
    """audit_logs テーブルを作成する（べき等）。"""
    with engine.connect() as conn:
        conn.execute(text("""
            CREATE TABLE IF NOT EXISTS audit_logs (
                id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
                actor_type    VARCHAR(20)  NOT NULL,
                actor_id      UUID         NOT NULL,
                actor_email   VARCHAR(255) NOT NULL,
                tenant_id     UUID         REFERENCES tenants(id) ON DELETE SET NULL,
                action        VARCHAR(100) NOT NULL,
                resource_type VARCHAR(50),
                resource_id   VARCHAR(255),
                detail        JSONB,
                ip_address    VARCHAR(45),
                result        VARCHAR(10)  NOT NULL DEFAULT 'success',
                created_at    TIMESTAMPTZ  NOT NULL DEFAULT now()
            )
        """))
        conn.execute(text("""
            CREATE INDEX IF NOT EXISTS ix_audit_logs_tenant_created
                ON audit_logs(tenant_id, created_at DESC)
        """))
        conn.execute(text("""
            CREATE INDEX IF NOT EXISTS ix_audit_logs_created
                ON audit_logs(created_at DESC)
        """))
        conn.commit()
```

- [ ] **Step 4: `app/services/audit.py` を新規作成する**

```python
import threading
import time
import uuid as _uuid
from datetime import datetime, timedelta, timezone

from sqlalchemy.orm import Session

from app.models.public import AuditLog


def write_audit_log(
    db: Session,
    actor_type: str,
    actor_id: str,
    actor_email: str,
    action: str,
    *,
    tenant_id: str | None = None,
    resource_type: str | None = None,
    resource_id: str | None = None,
    detail: dict | None = None,
    ip_address: str | None = None,
    result: str = "success",
) -> None:
    """監査ログを現在の DB セッションに追加する。commit は呼び出し側が行う。"""
    db.add(AuditLog(
        id=_uuid.uuid4(),
        actor_type=actor_type,
        actor_id=_uuid.UUID(actor_id),
        actor_email=actor_email,
        tenant_id=_uuid.UUID(tenant_id) if tenant_id else None,
        action=action,
        resource_type=resource_type,
        resource_id=resource_id,
        detail=detail,
        ip_address=ip_address,
        result=result,
    ))


def log_audit(
    actor_type: str,
    actor_id: str,
    actor_email: str,
    action: str,
    **kwargs,
) -> None:
    """セッションがないパス用。内部で Session を作りcommit・例外をすべて吸収する。"""
    try:
        from app.database import SessionLocal
        with SessionLocal() as db:
            write_audit_log(db, actor_type, actor_id, actor_email, action, **kwargs)
            db.commit()
    except Exception:
        pass


def _purge_old_audit_logs() -> None:
    from app.database import SessionLocal
    from app.config import settings
    cutoff = datetime.now(timezone.utc) - timedelta(days=settings.audit_log_retention_days)
    try:
        with SessionLocal() as db:
            db.query(AuditLog).filter(AuditLog.created_at < cutoff).delete()
            db.commit()
    except Exception:
        pass


def start_audit_purge_worker() -> None:
    """毎日 1 回古い監査ログを削除するバックグラウンドスレッドを起動する。"""
    def _loop() -> None:
        while True:
            _purge_old_audit_logs()
            time.sleep(86400)
    threading.Thread(target=_loop, daemon=True).start()
```

- [ ] **Step 5: `main.py` を更新する**

`core-api/app/main.py` の import 行を更新：

```python
from app.database import migrate_add_grafana_org_id, migrate_add_device_name, migrate_add_provisioning_token_id, migrate_add_public_token, migrate_add_token_version, migrate_totp_columns, migrate_dashboard_panel_configs, migrate_create_audit_logs
from app.services.audit import start_audit_purge_worker
```

`on_startup()` のマイグレーションリストに `migrate_create_audit_logs` を追加、末尾にpurge worker起動を追加：

```python
@app.on_event("startup")
def on_startup():
    for migrate in (migrate_add_grafana_org_id, migrate_add_device_name, migrate_add_provisioning_token_id, migrate_add_public_token, migrate_add_token_version, migrate_totp_columns, migrate_dashboard_panel_configs, migrate_create_audit_logs):
        try:
            migrate()
        except Exception as e:
            print(f"Migration warning: {e}")
    threading.Thread(target=_run_emqx_setup, daemon=True).start()
    start_audit_purge_worker()
```

- [ ] **Step 6: テストを書く**

`core-api/tests/test_audit.py` を新規作成：

```python
from unittest.mock import MagicMock, patch
import uuid
from app.services.audit import write_audit_log, log_audit


def test_write_audit_log_adds_to_session():
    mock_db = MagicMock()
    write_audit_log(
        mock_db,
        actor_type="platform",
        actor_id=str(uuid.uuid4()),
        actor_email="admin@example.com",
        action="login_success",
        ip_address="127.0.0.1",
    )
    mock_db.add.assert_called_once()
    added = mock_db.add.call_args[0][0]
    assert added.actor_type == "platform"
    assert added.actor_email == "admin@example.com"
    assert added.action == "login_success"
    assert added.result == "success"


def test_write_audit_log_failure_result():
    mock_db = MagicMock()
    write_audit_log(
        mock_db,
        actor_type="tenant",
        actor_id=str(uuid.uuid4()),
        actor_email="user@tenant.com",
        action="login_failure",
        result="failure",
    )
    added = mock_db.add.call_args[0][0]
    assert added.result == "failure"


def test_write_audit_log_with_tenant_id():
    mock_db = MagicMock()
    tid = str(uuid.uuid4())
    write_audit_log(
        mock_db,
        actor_type="tenant",
        actor_id=str(uuid.uuid4()),
        actor_email="user@tenant.com",
        action="create_device",
        tenant_id=tid,
        resource_type="device",
        resource_id="device-001",
    )
    added = mock_db.add.call_args[0][0]
    assert str(added.tenant_id) == tid
    assert added.resource_type == "device"
    assert added.resource_id == "device-001"


def test_log_audit_swallows_exceptions():
    """DB 接続が失敗しても例外を外に出さないこと。"""
    with patch("app.services.audit.SessionLocal", side_effect=Exception("DB down")):
        log_audit("platform", str(uuid.uuid4()), "a@b.com", "login_success")
        # 例外が発生しないことを確認（ここに到達すれば OK）
```

- [ ] **Step 7: テストを実行して全て通ることを確認する**

```bash
cd core-api
pytest tests/test_audit.py -v
```

Expected: 4 tests PASS

- [ ] **Step 8: コミットする**

```bash
git add core-api/app/models/public.py core-api/app/config.py core-api/app/database.py \
        core-api/app/services/audit.py core-api/app/main.py core-api/tests/test_audit.py
git commit -m "feat(audit): 監査ログ DBモデル・マイグレーション・サービス基盤を追加"
```

---

### Task 2: PF管理者APIエンドポイント

**Files:**
- Create: `core-api/app/schemas/audit.py`
- Create: `core-api/app/routers/audit_logs.py`
- Modify: `core-api/app/main.py`
- Test: `core-api/tests/test_audit_api.py`

**Interfaces:**
- Consumes: `write_audit_log` / `log_audit` from `app.services.audit` (Task 1)
- Consumes: `AuditLog` model from `app.models.public` (Task 1)
- Produces: `GET /audit-logs` — PF管理者が監査ログを一覧取得するエンドポイント

- [ ] **Step 1: `app/schemas/audit.py` を新規作成する**

```python
from datetime import datetime
from pydantic import BaseModel, ConfigDict


class AuditLogOut(BaseModel):
    id:            str
    actor_type:    str
    actor_email:   str
    tenant_id:     str | None
    action:        str
    resource_type: str | None
    resource_id:   str | None
    detail:        dict | None
    ip_address:    str | None
    result:        str
    created_at:    datetime

    model_config = ConfigDict(from_attributes=True)


class AuditLogListOut(BaseModel):
    total: int
    items: list[AuditLogOut]
```

- [ ] **Step 2: `app/routers/audit_logs.py` を新規作成する**

```python
from datetime import datetime
from fastapi import APIRouter, Depends, HTTPException, Query, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials
from sqlalchemy import func

from app.database import SessionLocal
from app.models.public import AuditLog, Tenant
from app.schemas.audit import AuditLogListOut, AuditLogOut
from app.services.auth import verify_token

router = APIRouter(prefix="/audit-logs", tags=["audit-logs"])
_bearer = HTTPBearer()


def _require_platform(creds: HTTPAuthorizationCredentials = Depends(_bearer)) -> dict:
    payload = verify_token(creds.credentials)
    if not payload or payload.get("type") != "platform" or payload.get("token_type") == "refresh":
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Unauthorized")
    return payload


@router.get("", response_model=AuditLogListOut)
def list_audit_logs(
    tenant_id: str | None = Query(default=None),
    action:    str | None = Query(default=None),
    from_dt:   datetime | None = Query(default=None),
    to_dt:     datetime | None = Query(default=None),
    limit:     int = Query(default=50, ge=1, le=100),
    offset:    int = Query(default=0, ge=0),
    _: dict = Depends(_require_platform),
):
    with SessionLocal() as db:
        q = db.query(AuditLog)
        if tenant_id:
            q = q.filter(AuditLog.tenant_id == tenant_id)
        if action:
            q = q.filter(AuditLog.action == action)
        if from_dt:
            q = q.filter(AuditLog.created_at >= from_dt)
        if to_dt:
            q = q.filter(AuditLog.created_at <= to_dt)
        total = q.count()
        rows = q.order_by(AuditLog.created_at.desc()).offset(offset).limit(limit).all()
        items = [
            AuditLogOut(
                id=str(r.id),
                actor_type=r.actor_type,
                actor_email=r.actor_email,
                tenant_id=str(r.tenant_id) if r.tenant_id else None,
                action=r.action,
                resource_type=r.resource_type,
                resource_id=r.resource_id,
                detail=r.detail,
                ip_address=r.ip_address,
                result=r.result,
                created_at=r.created_at,
            )
            for r in rows
        ]
    return AuditLogListOut(total=total, items=items)
```

- [ ] **Step 3: `main.py` にルーターを登録する**

`core-api/app/main.py` の import 行に `audit_logs` を追加：

```python
from app.routers import health, auth, mfa, tenants, provisioning, emqx, provisioning_tokens, alert_rules, emqx_events, firmware, stats, tenant_auth, tenant_mfa, tenant_users, tenant_devices, tenant_grafana, tenant_portal, public_access, platform, audit_logs
```

`app.include_router(public_access.router)` の後に追加：

```python
app.include_router(audit_logs.router)
```

- [ ] **Step 4: テストを書く**

`core-api/tests/test_audit_api.py` を新規作成：

```python
from unittest.mock import patch, MagicMock
from datetime import datetime, timezone
import uuid


def _make_token(client, user_id="user-1", email="admin@example.com"):
    from app.services.auth import create_access_token
    return create_access_token({"sub": user_id, "email": email, "type": "platform"})


def test_list_audit_logs_requires_auth(client):
    resp = client.get("/audit-logs")
    assert resp.status_code == 403  # no Bearer token


def test_list_audit_logs_empty(client):
    token = _make_token(client)
    with patch("app.routers.audit_logs.SessionLocal") as mock_sl:
        mock_db = MagicMock()
        mock_db.__enter__ = lambda s: mock_db
        mock_db.__exit__ = MagicMock(return_value=False)
        mock_sl.return_value = mock_db
        mock_db.query.return_value.filter.return_value.count.return_value = 0
        mock_db.query.return_value.count.return_value = 0
        mock_db.query.return_value.order_by.return_value.offset.return_value.limit.return_value.all.return_value = []
        resp = client.get("/audit-logs", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200
    data = resp.json()
    assert data["total"] == 0
    assert data["items"] == []


def test_list_audit_logs_limit_max(client):
    token = _make_token(client)
    resp = client.get("/audit-logs?limit=200", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 422  # limit > 100 は validation error
```

- [ ] **Step 5: テストを実行して全て通ることを確認する**

```bash
cd core-api
pytest tests/test_audit_api.py -v
```

Expected: 3 tests PASS

- [ ] **Step 6: コミットする**

```bash
git add core-api/app/schemas/audit.py core-api/app/routers/audit_logs.py \
        core-api/app/main.py core-api/tests/test_audit_api.py
git commit -m "feat(audit): PF管理者向け監査ログAPIエンドポイントを追加"
```

---

### Task 3: テナントポータルAPIエンドポイント

**Files:**
- Modify: `core-api/app/routers/tenant_portal.py`
- Test: `core-api/tests/test_audit_api.py`（追記）

**Interfaces:**
- Consumes: `AuditLog` model (Task 1)、`AuditLogListOut`, `AuditLogOut` schema (Task 2)
- Consumes: `_require_admin(payload)` — `tenant_portal.py` の既存 Dependency、`payload["tenant_id"]` でテナントIDを取得
- Produces: `GET /tenant-portal/audit-logs` — テナント管理者が自テナントの監査ログを取得

- [ ] **Step 1: テストを書く（失敗することを確認）**

`core-api/tests/test_audit_api.py` に追記：

```python
def _make_tenant_token(client, user_id="user-1", email="admin@tenant.com", tenant_id=None, role="admin"):
    from app.services.auth import create_access_token
    tid = tenant_id or str(uuid.uuid4())
    return create_access_token({"sub": user_id, "email": email, "type": "tenant",
                                 "tenant_id": tid, "role": role}), tid


def test_tenant_audit_logs_requires_admin(client):
    from app.services.auth import create_access_token
    token = create_access_token({"sub": "u1", "email": "e@t.com", "type": "tenant",
                                  "tenant_id": str(uuid.uuid4()), "role": "viewer"})
    resp = client.get("/tenant-portal/audit-logs",
                      cookies={"iot_token": token})
    assert resp.status_code == 403


def test_tenant_audit_logs_empty(client):
    token, tid = _make_tenant_token(client)
    with patch("app.routers.tenant_portal.SessionLocal") as mock_sl:
        mock_db = MagicMock()
        mock_db.__enter__ = lambda s: mock_db
        mock_db.__exit__ = MagicMock(return_value=False)
        mock_sl.return_value = mock_db
        mock_db.query.return_value.filter.return_value.count.return_value = 0
        mock_db.query.return_value.filter.return_value.order_by.return_value.offset.return_value.limit.return_value.all.return_value = []
        resp = client.get("/tenant-portal/audit-logs", cookies={"iot_token": token})
    assert resp.status_code == 200
    data = resp.json()
    assert data["total"] == 0
```

- [ ] **Step 2: テストを実行して失敗することを確認する**

```bash
cd core-api
pytest tests/test_audit_api.py::test_tenant_audit_logs_empty -v
```

Expected: FAIL with 404 or AttributeError（エンドポイントがまだ存在しない）

- [ ] **Step 3: `tenant_portal.py` に `GET /tenant-portal/audit-logs` を追加する**

`tenant_portal.py` の import 行に追加：

```python
from app.models.public import AuditLog
from app.schemas.audit import AuditLogListOut, AuditLogOut
from datetime import datetime
from fastapi import Query
```

ファイル末尾に追加：

```python
@router.get("/audit-logs", response_model=AuditLogListOut)
def get_tenant_audit_logs(
    action:  str | None = Query(default=None),
    from_dt: datetime | None = Query(default=None),
    to_dt:   datetime | None = Query(default=None),
    limit:   int = Query(default=50, ge=1, le=100),
    offset:  int = Query(default=0, ge=0),
    payload: dict = Depends(_require_admin),
):
    tenant_id = payload["tenant_id"]
    with SessionLocal() as db:
        q = db.query(AuditLog).filter(AuditLog.tenant_id == tenant_id)
        if action:
            q = q.filter(AuditLog.action == action)
        if from_dt:
            q = q.filter(AuditLog.created_at >= from_dt)
        if to_dt:
            q = q.filter(AuditLog.created_at <= to_dt)
        total = q.count()
        rows = q.order_by(AuditLog.created_at.desc()).offset(offset).limit(limit).all()
        items = [
            AuditLogOut(
                id=str(r.id),
                actor_type=r.actor_type,
                actor_email=r.actor_email,
                tenant_id=str(r.tenant_id) if r.tenant_id else None,
                action=r.action,
                resource_type=r.resource_type,
                resource_id=r.resource_id,
                detail=r.detail,
                ip_address=r.ip_address,
                result=r.result,
                created_at=r.created_at,
            )
            for r in rows
        ]
    return AuditLogListOut(total=total, items=items)
```

- [ ] **Step 4: テストを実行して全て通ることを確認する**

```bash
cd core-api
pytest tests/test_audit_api.py -v
```

Expected: 全テスト PASS

- [ ] **Step 5: コミットする**

```bash
git add core-api/app/routers/tenant_portal.py core-api/tests/test_audit_api.py
git commit -m "feat(audit): テナントポータル向け監査ログAPIエンドポイントを追加"
```

---

### Task 4: 認証・MFA系ルーターへのログ記録追加

**Files:**
- Modify: `core-api/app/routers/auth.py`
- Modify: `core-api/app/routers/tenant_auth.py`
- Modify: `core-api/app/routers/mfa.py`
- Modify: `core-api/app/routers/tenant_mfa.py`
- Modify: `core-api/app/routers/platform.py`

**Interfaces:**
- Consumes: `log_audit(actor_type, actor_id, actor_email, action, **kwargs)` from `app.services.audit` (Task 1)
- Consumes: `write_audit_log(db, actor_type, actor_id, actor_email, action, **kwargs)` (Task 1)

各ファイルの先頭に以下の import を追加する（ファイルごとに必要なものを選んで追加）：

```python
import uuid as _audit_uuid
from app.services.audit import log_audit, write_audit_log
```

- [ ] **Step 1: `auth.py` の `login` エンドポイントにログ追加**

`auth.py:login` 関数の `record_failure(rate_key)` 行の後（`raise HTTPException` の前）に追加：

```python
    if not user or not user.is_active or not verify_password(req.password, user.password_hash):
        record_failure(rate_key)
        actor_id  = str(user.id) if (user and user.is_active) else str(_audit_uuid.UUID(int=0))
        actor_email = user.email if (user and user.is_active) else req.email
        log_audit("platform", actor_id, actor_email, "login_failure",
                  ip_address=ip, result="failure")
        raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid credentials")
    clear_failures(rate_key)
    log_audit("platform", str(user.id), user.email, "login_success", ip_address=ip)
```

- [ ] **Step 2: `auth.py` の `logout` エンドポイントにログ追加**

`logout` 関数の `response.delete_cookie(...)` の後に追加：

```python
    if req.refresh_token:
        payload_data = verify_token(req.refresh_token)
        if payload_data and payload_data.get("token_type") == "refresh":
            # ... 既存のrevoke処理 ...
            log_audit("platform", payload_data["sub"], payload_data["email"], "logout")
```

注: 既存の `logout` 関数は `req.refresh_token` を検証してから revoke する。既存コードを書き換えず、revoke処理の後にログ追加：

```python
@router.post("/logout", status_code=status.HTTP_204_NO_CONTENT)
def logout(req: LogoutRequest, response: Response):
    if req.refresh_token:
        payload_data = verify_token(req.refresh_token)
        if payload_data and payload_data.get("token_type") == "refresh":
            jti = payload_data.get("jti")
            if jti:
                exp = payload_data.get("exp", 0)
                ttl = max(0.0, exp - datetime.now(timezone.utc).timestamp())
                revoke_jti(jti, ttl)
            log_audit("platform", payload_data["sub"], payload_data["email"], "logout")
    response.delete_cookie(key="iot_token", path="/")
```

- [ ] **Step 3: `auth.py` の `change_password` エンドポイントにログ追加**

`auth.py:change_password` の `db.commit()` の直前に追加：

```python
        user.password_hash = hash_password(req.new_password)
        user.token_version = user.token_version + 1
        write_audit_log(db, "platform", str(user.id), user.email, "change_password",
                        resource_type="platform_user", resource_id=user.email)
        db.commit()
```

- [ ] **Step 4: `tenant_auth.py` の `tenant_login` にログ追加**

`tenant_auth.py:tenant_login` 関数。ログイン失敗は2箇所ある（tenant not found と user not found/wrong password）。それぞれの `raise HTTPException` 直前に追加：

```python
    # tenant が見つからない場合
    if not tenant:
        _equalize_timing(req.password)
        record_failure(rate_key)
        log_audit("tenant", str(_audit_uuid.UUID(int=0)), req.email, "login_failure",
                  result="failure", ip_address=ip)
        raise HTTPException(...)

    # user が見つからない場合
    if not row:
        _equalize_timing(req.password)
        record_failure(rate_key)
        log_audit("tenant", str(_audit_uuid.UUID(int=0)), req.email, "login_failure",
                  tenant_id=str(tenant.id), result="failure", ip_address=ip)
        raise HTTPException(...)

    # パスワード不一致
    if not verify_password(req.password, row.password_hash):
        record_failure(rate_key)
        log_audit("tenant", str(row.id), row.email, "login_failure",
                  tenant_id=str(tenant.id), result="failure", ip_address=ip)
        raise HTTPException(...)

    clear_failures(rate_key)
    log_audit("tenant", str(row.id), row.email, "login_success",
              tenant_id=str(tenant.id), ip_address=ip)
```

また、`ip` 変数が必要なので関数先頭の `ip = request.client.host if request.client else "unknown"` は既に存在している（rate_key と同じ行付近を確認）。

- [ ] **Step 5: `tenant_auth.py` の `tenant_logout` にログ追加**

`tenant_logout` はCookieからトークンを検証できる：

```python
@router.post("/logout")
def tenant_logout(response: Response, iot_token: str | None = Cookie(default=None)):
    if iot_token:
        payload_data = verify_token(iot_token)
        if payload_data and payload_data.get("type") == "tenant":
            log_audit("tenant", payload_data["sub"], payload_data["email"], "logout",
                      tenant_id=payload_data.get("tenant_id"))
    response.delete_cookie(key="iot_token", path="/")
    return {"ok": True}
```

注: 現在の `tenant_logout` はCookieを受け取っていないため、引数に `iot_token: str | None = Cookie(default=None)` を追加する。

- [ ] **Step 6: `tenant_auth.py` の `change_password` にログ追加**

`change_password` 関数の `conn.commit()` の直前に追加：

```python
        conn.execute(
            text(f'UPDATE "{schema}".users SET password_hash = :hash WHERE id = :uid'),
            {"hash": hash_password(req.new_password), "uid": user_id},
        )
        conn.commit()
    log_audit("tenant", user_id, payload["email"], "change_password",
              tenant_id=tenant_id, resource_type="tenant_user", resource_id=payload["email"])
```

- [ ] **Step 7: `mfa.py` の `totp_activate` にログ追加**

`mfa.py:totp_activate` — TOTP有効化成功時（`user.totp_enabled = True` の後）：

```python
        user.totp_enabled = True
        user.totp_secret = secret
        user.token_version = user.token_version + 1
        write_audit_log(db, "platform", str(user.id), user.email, "totp_enabled",
                        resource_type="platform_user", resource_id=user.email)
        db.commit()
```

`mfa.py` の `totp_activate` が SessionLocal を使っているか確認（使っていれば write_audit_log、なければ log_audit を使う）。

- [ ] **Step 8: `platform.py` の `update_mfa_settings` にログ追加**

`platform.py:update_mfa_settings` の `db.commit()` 直前：

```python
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "mfa_settings_updated",
                        resource_type="mfa_settings",
                        detail={"platform_required": s.platform_required,
                                "tenant_required": s.tenant_required})
        db.commit()
```

注: `update_mfa_settings` の `_` パラメータを `payload: dict = Depends(_require_platform)` に変更する。

- [ ] **Step 9: 既存テストが全て通ることを確認する**

```bash
cd core-api
pytest tests/ -v --ignore=tests/test_audit.py --ignore=tests/test_audit_api.py
```

Expected: 全テスト PASS（既存テストに回帰なし）

- [ ] **Step 10: コミットする**

```bash
git add core-api/app/routers/auth.py core-api/app/routers/tenant_auth.py \
        core-api/app/routers/mfa.py core-api/app/routers/tenant_mfa.py \
        core-api/app/routers/platform.py
git commit -m "feat(audit): 認証・MFA系操作の監査ログ記録を追加"
```

---

### Task 5: リソース操作系ルーターへのログ記録追加

**Files:**
- Modify: `core-api/app/routers/tenants.py`
- Modify: `core-api/app/routers/tenant_users.py`
- Modify: `core-api/app/routers/tenant_devices.py`
- Modify: `core-api/app/routers/firmware.py`
- Modify: `core-api/app/routers/provisioning_tokens.py`
- Modify: `core-api/app/routers/tenant_portal.py`（デバイス・ファームウェア・トークン操作）

**Interfaces:**
- Consumes: `write_audit_log(db, ...)` from `app.services.audit` (Task 1)
- Consumes: 各ルーターの `_require_platform` Dependency の返り値（`payload` dict）

全ファイルの import 行に追加：

```python
from app.services.audit import write_audit_log
```

**方針:** 現在 `_: dict = Depends(_require_platform)` と破棄しているパラメータを `payload: dict = Depends(_require_platform)` に変更してから `write_audit_log` を呼ぶ。

- [ ] **Step 1: `tenants.py` に監査ログを追加する**

各エンドポイントの `_` を `payload` に変更し、`db.commit()` 直前に `write_audit_log` を追加：

`create_tenant`:
```python
def create_tenant(body: TenantCreate, payload: dict = Depends(_require_platform)):
    with SessionLocal() as db:
        ...
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "create_tenant", resource_type="tenant", resource_id=body.name)
        db.commit()
```

`update_tenant`:
```python
def update_tenant(tenant_id: str, body: TenantUpdate, payload: dict = Depends(_require_platform)):
    with SessionLocal() as db:
        ...
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "update_tenant", resource_type="tenant", resource_id=tenant.name)
        db.commit()
```

`delete_tenant`:
```python
def delete_tenant(tenant_id: str, background_tasks: BackgroundTasks, payload: dict = Depends(_require_platform)):
    with SessionLocal() as db:
        ...
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "delete_tenant", resource_type="tenant", resource_id=tenant.name)
        db.commit()
```

`update_tenant_status`:
```python
def update_tenant_status(tenant_id: str, body: TenantStatusUpdate, payload: dict = Depends(_require_platform)):
    with SessionLocal() as db:
        ...
        action = "suspend_tenant" if body.status == "suspended" else "activate_tenant"
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        action, resource_type="tenant", resource_id=tenant.name)
        db.commit()
```

- [ ] **Step 2: `tenant_users.py` に監査ログを追加する**

`create_tenant_user` の `conn.commit()` 直前（`ensure_grafana_user_in_org` の後）：
```python
def create_tenant_user(tenant_id: UUID, body: TenantUserCreate, payload: dict = Depends(_require_platform)):
    ...
    with engine.connect() as conn:
        try:
            conn.execute(...)
            if tenant.grafana_org_id:
                ensure_grafana_user_in_org(...)
            # audit
            from app.services.audit import write_audit_log as _wal
            from app.database import SessionLocal as _SL
            with _SL() as log_db:
                _wal(log_db, "platform", payload["sub"], payload["email"],
                     "create_tenant_user", tenant_id=tenant_id_str,
                     resource_type="tenant_user", resource_id=body.email)
                log_db.commit()
            conn.commit()
```

注: `tenant_users.py` は `engine.connect()` を使用しており SessionLocal ではないため、別途 `SessionLocal` を使って audit log をコミットする。

`delete_tenant_user` にも同様のパターンで追加（tenant_users.py の該当エンドポイントを確認して適用）。

- [ ] **Step 3: `tenant_devices.py` の `delete_tenant_device` に監査ログを追加する**

```python
def delete_tenant_device(tenant_id: UUID, device_id: str, payload: dict = Depends(_require_platform)):
    tenant_id_str = str(tenant_id)
    tenant = _get_active_tenant(tenant_id_str)
    schema = f"tenant_{tenant_id_str.replace('-', '_')}"
    with engine.connect() as conn:
        row = conn.execute(...)
        if not row:
            raise HTTPException(...)
        device_name = row.device_name or device_id
        conn.execute(text(f'DELETE FROM "{schema}".devices WHERE device_id = :did'), {"did": device_id})
        conn.commit()
    from app.services.audit import log_audit
    log_audit("platform", payload["sub"], payload["email"], "delete_device",
              tenant_id=tenant_id_str, resource_type="device", resource_id=device_id)
    if tenant.influxdb_org_id:
        retire_device_in_influxdb(tenant.influxdb_org_id, device_name)
```

- [ ] **Step 4: `firmware.py` に監査ログを追加する**

`upload_firmware_release` の `db.commit()` 直前：
```python
def upload_firmware_release(..., payload: dict = Depends(_require_platform)):
    ...
    with SessionLocal() as db:
        db.execute(text(f'''INSERT INTO ...'''), {...})
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "upload_firmware", tenant_id=tenant_id,
                        resource_type="firmware", resource_id=version)
        db.commit()
```

`deactivate_firmware` の `db.commit()` 直前：
```python
def deactivate_firmware(tenant_id: str, firmware_id: str, payload: dict = Depends(_require_platform)):
    ...
    with SessionLocal() as db:
        result = db.execute(...)
        db.commit()
        if result.rowcount == 0:
            raise HTTPException(...)
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "delete_firmware", tenant_id=tenant_id,
                        resource_type="firmware", resource_id=firmware_id)
        db.commit()
```

注: `delete_firmware` は `is_active = FALSE` にするだけ（物理削除ではない）。

`dispatch_ota_command` の `publish_ota_command` 呼び出しの後：
```python
def dispatch_ota_command(..., payload: dict = Depends(_require_platform)):
    ...
    publish_ota_command(tenant_id, device_id, {...})
    from app.services.audit import log_audit
    log_audit("platform", payload["sub"], payload["email"], "ota_send",
              tenant_id=tenant_id, resource_type="device", resource_id=device_id,
              detail={"firmware_id": firmware_id, "version": row.version})
    return {...}
```

- [ ] **Step 5: `provisioning_tokens.py` に監査ログを追加する**

`create_provisioning_token` の `db.commit()` 直前：
```python
def create_provisioning_token(tenant_id: str, body: TokenCreate, payload: dict = Depends(_require_platform)):
    ...
    with SessionLocal() as db:
        token = ProvisioningToken(...)
        db.add(token)
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "create_provisioning_token", tenant_id=tenant_id,
                        resource_type="provisioning_token", resource_id=str(token.id))
        db.commit()
```

`revoke_provisioning_token` の `db.commit()` 直前：
```python
def revoke_provisioning_token(tenant_id: str, token_id: str, payload: dict = Depends(_require_platform)):
    ...
    with SessionLocal() as db:
        token = db.query(ProvisioningToken)...
        if not token:
            raise HTTPException(...)
        token.is_active = False
        write_audit_log(db, "platform", payload["sub"], payload["email"],
                        "delete_provisioning_token", tenant_id=tenant_id,
                        resource_type="provisioning_token", resource_id=token_id)
        db.commit()
```

- [ ] **Step 6: `tenant_portal.py` のテナント操作系にログを追加する**

`tenant_portal.py` はCookieベース認証で `_require_admin` / `_require_tenant` が payload を返す。
デバイス削除・ファームウェアアップロード・削除・OTA・プロビジョニングトークン作成/削除の各エンドポイントを確認し、`write_audit_log` または `log_audit` を追加する。

パターン（SessionLocal を使うエンドポイントの場合）：
```python
@router.delete("/devices/{device_id}", ...)
def delete_device(device_id: str, ..., payload: dict = Depends(_require_admin_or_operator)):
    ...
    with SessionLocal() as db:  # 既存のセッションがあれば使う
        ...
        write_audit_log(db, "tenant", payload["sub"], payload["email"],
                        "delete_device", tenant_id=payload["tenant_id"],
                        resource_type="device", resource_id=device_id)
        db.commit()
```

`engine.connect()` のみ使うエンドポイントの場合：
```python
    conn.commit()
    log_audit("tenant", payload["sub"], payload["email"], "delete_device",
              tenant_id=payload["tenant_id"], resource_type="device", resource_id=device_id)
```

`tenant_portal.py` の各操作エンドポイントを確認し、上記パターンを適用する。対象エンドポイント：
- デバイス削除（`DELETE /tenant-portal/devices/{device_id}`）
- ファームウェアアップロード（`POST /tenant-portal/firmware`）
- ファームウェア削除（`DELETE /tenant-portal/firmware/{firmware_id}`）
- OTA送信（`POST /tenant-portal/devices/{device_id}/ota`）
- プロビジョニングトークン作成（`POST /tenant-portal/provisioning-tokens`）
- プロビジョニングトークン削除（`DELETE /tenant-portal/provisioning-tokens/{token_id}`）
- テナントユーザー作成（`POST /tenant-portal/users`）
- テナントユーザー削除（`DELETE /tenant-portal/users/{user_id}`）

- [ ] **Step 7: 既存テストが全て通ることを確認する**

```bash
cd core-api
pytest tests/ -v
```

Expected: 全テスト PASS

- [ ] **Step 8: コミットする**

```bash
git add core-api/app/routers/tenants.py core-api/app/routers/tenant_users.py \
        core-api/app/routers/tenant_devices.py core-api/app/routers/firmware.py \
        core-api/app/routers/provisioning_tokens.py core-api/app/routers/tenant_portal.py
git commit -m "feat(audit): リソース操作系ルーターへの監査ログ記録を追加"
```

---

### Task 6: PF管理者向け監査ログUI

**Files:**
- Create: `admin-ui/audit-logs.html`
- Modify: `admin-ui/tenants.html`（ナビに監査ログリンク追加）
- Modify: `admin-ui/platform-settings.html`（同様）

**Interfaces:**
- Consumes: `GET /audit-logs?tenant_id=&action=&from_dt=&to_dt=&limit=&offset=` (Task 2)

- [ ] **Step 1: `admin-ui/audit-logs.html` を新規作成する**

```html
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>監査ログ - IoTAir-X</title>
  <link rel="stylesheet" href="/admin/static/tailwind.css">
  <script src="/admin/static/alpine.min.js" defer></script>
  <script src="/admin/js/api.js?v=10"></script>
</head>
<body class="bg-gray-50 min-h-screen">

  <!-- ナビゲーション（tenants.html と同一構造） -->
  <nav class="bg-white border-b border-gray-200 px-6 py-3 flex items-center justify-between">
    <img src="/admin/IoTAirx_logo.png" alt="IoTAirx" class="h-8 w-auto">
    <div class="flex items-center gap-4">
      <a href="/admin/tenants.html"           class="text-sm text-gray-500 hover:text-gray-700">テナント管理</a>
      <a href="/admin/admin-change-password.html" class="text-sm text-gray-500 hover:text-gray-700">パスワード変更</a>
      <a href="/admin/platform-settings.html" class="text-sm text-gray-500 hover:text-gray-700">設定</a>
      <button onclick="api.logout()" class="text-sm text-gray-500 hover:text-gray-700">ログアウト</button>
    </div>
  </nav>

  <div x-data="auditLogsApp()" x-init="init()" class="max-w-7xl mx-auto px-6 py-8">
    <h2 class="text-xl font-semibold text-gray-800 mb-6">監査ログ</h2>

    <!-- フィルター -->
    <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-4 mb-6">
      <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
        <div>
          <label class="block text-xs font-medium text-gray-600 mb-1">テナント</label>
          <select x-model="filter.tenant_id"
                  class="w-full border border-gray-300 rounded px-2 py-1.5 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500">
            <option value="">すべて</option>
            <template x-for="t in tenants" :key="t.id">
              <option :value="t.id" x-text="t.name"></option>
            </template>
          </select>
        </div>
        <div>
          <label class="block text-xs font-medium text-gray-600 mb-1">アクション</label>
          <select x-model="filter.action"
                  class="w-full border border-gray-300 rounded px-2 py-1.5 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500">
            <option value="">すべて</option>
            <option value="login_success">ログイン成功</option>
            <option value="login_failure">ログイン失敗</option>
            <option value="logout">ログアウト</option>
            <option value="create_tenant">テナント作成</option>
            <option value="delete_tenant">テナント削除</option>
            <option value="create_device">デバイス作成</option>
            <option value="delete_device">デバイス削除</option>
            <option value="upload_firmware">FWアップロード</option>
            <option value="delete_firmware">FW削除</option>
            <option value="ota_send">OTA送信</option>
            <option value="create_provisioning_token">トークン作成</option>
            <option value="delete_provisioning_token">トークン削除</option>
          </select>
        </div>
        <div>
          <label class="block text-xs font-medium text-gray-600 mb-1">開始日</label>
          <input type="date" x-model="filter.from_dt"
                 class="w-full border border-gray-300 rounded px-2 py-1.5 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
        <div>
          <label class="block text-xs font-medium text-gray-600 mb-1">終了日</label>
          <input type="date" x-model="filter.to_dt"
                 class="w-full border border-gray-300 rounded px-2 py-1.5 text-sm focus:outline-none focus:ring-2 focus:ring-blue-500">
        </div>
      </div>
      <div class="mt-3 flex justify-end">
        <button @click="search()"
                class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-1.5 rounded text-sm font-medium">
          検索
        </button>
      </div>
    </div>

    <!-- エラー -->
    <div x-show="error" class="bg-red-50 border border-red-200 text-red-600 text-sm rounded px-3 py-2 mb-4" x-text="error"></div>

    <!-- テーブル -->
    <div class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
      <div x-show="loading" class="text-center py-8 text-gray-400 text-sm">読み込み中...</div>
      <div x-show="!loading" class="overflow-x-auto">
        <table class="w-full text-sm">
          <thead class="bg-gray-50 border-b border-gray-200">
            <tr>
              <th class="text-left px-4 py-3 font-medium text-gray-600 whitespace-nowrap">日時</th>
              <th class="text-left px-4 py-3 font-medium text-gray-600">操作者</th>
              <th class="text-left px-4 py-3 font-medium text-gray-600">テナント</th>
              <th class="text-left px-4 py-3 font-medium text-gray-600">アクション</th>
              <th class="text-left px-4 py-3 font-medium text-gray-600">対象</th>
              <th class="text-left px-4 py-3 font-medium text-gray-600">結果</th>
              <th class="text-left px-4 py-3 font-medium text-gray-600">IP</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-100">
            <template x-if="logs.length === 0">
              <tr>
                <td colspan="7" class="text-center py-8 text-gray-400">ログがありません</td>
              </tr>
            </template>
            <template x-for="log in logs" :key="log.id">
              <tr class="hover:bg-gray-50">
                <td class="px-4 py-3 whitespace-nowrap text-gray-600 text-xs" x-text="formatDate(log.created_at)"></td>
                <td class="px-4 py-3 text-gray-700" x-text="log.actor_email"></td>
                <td class="px-4 py-3 text-gray-600 text-xs" x-text="tenantName(log.tenant_id)"></td>
                <td class="px-4 py-3">
                  <span class="font-mono text-xs bg-gray-100 px-1.5 py-0.5 rounded" x-text="log.action"></span>
                </td>
                <td class="px-4 py-3 text-gray-600 text-xs" x-text="log.resource_id || '—'"></td>
                <td class="px-4 py-3">
                  <span :class="log.result === 'success' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-600'"
                        class="text-xs px-2 py-0.5 rounded-full font-medium"
                        x-text="log.result === 'success' ? '成功' : '失敗'"></span>
                </td>
                <td class="px-4 py-3 text-gray-500 text-xs" x-text="log.ip_address || '—'"></td>
              </tr>
            </template>
          </tbody>
        </table>
      </div>

      <!-- ページネーション -->
      <div x-show="!loading && total > 0"
           class="flex items-center justify-between px-4 py-3 border-t border-gray-200">
        <span class="text-sm text-gray-500">
          全 <span x-text="total"></span> 件中
          <span x-text="offset + 1"></span>–<span x-text="Math.min(offset + limit, total)"></span> 件
        </span>
        <div class="flex gap-2">
          <button @click="prevPage()" :disabled="offset === 0"
                  class="px-3 py-1 text-sm border border-gray-300 rounded hover:bg-gray-50 disabled:opacity-40">
            前へ
          </button>
          <button @click="nextPage()" :disabled="offset + limit >= total"
                  class="px-3 py-1 text-sm border border-gray-300 rounded hover:bg-gray-50 disabled:opacity-40">
            次へ
          </button>
        </div>
      </div>
    </div>
  </div>

  <script>
  function auditLogsApp() {
    return {
      logs: [],
      total: 0,
      loading: false,
      error: '',
      offset: 0,
      limit: 50,
      tenants: [],
      filter: { tenant_id: '', action: '', from_dt: '', to_dt: '' },

      async init() {
        await this.loadTenants();
        await this.search();
      },

      async loadTenants() {
        try {
          this.tenants = await api.get('/tenants');
        } catch(e) {
          // テナント一覧取得失敗は無視
        }
      },

      tenantName(tenant_id) {
        if (!tenant_id) return '（PF）';
        const t = this.tenants.find(t => t.id === tenant_id);
        return t ? t.name : tenant_id.slice(0, 8) + '…';
      },

      formatDate(iso) {
        return new Date(iso).toLocaleString('ja-JP', { timeZoneName: 'short' });
      },

      async search() {
        this.offset = 0;
        await this.load();
      },

      async load() {
        this.loading = true;
        this.error = '';
        try {
          const params = new URLSearchParams({ limit: this.limit, offset: this.offset });
          if (this.filter.tenant_id) params.set('tenant_id', this.filter.tenant_id);
          if (this.filter.action)    params.set('action',    this.filter.action);
          if (this.filter.from_dt)   params.set('from_dt',   this.filter.from_dt + 'T00:00:00Z');
          if (this.filter.to_dt)     params.set('to_dt',     this.filter.to_dt   + 'T23:59:59Z');
          const data = await api.get('/audit-logs?' + params.toString());
          this.logs  = data.items;
          this.total = data.total;
        } catch(e) {
          this.error = e.message || '読み込みに失敗しました';
        } finally {
          this.loading = false;
        }
      },

      async prevPage() { this.offset = Math.max(0, this.offset - this.limit); await this.load(); },
      async nextPage() { this.offset += this.limit; await this.load(); },
    };
  }
  </script>
</body>
</html>
```

- [ ] **Step 2: 既存ナビゲーションに「監査ログ」リンクを追加する**

`admin-ui/tenants.html` と `admin-ui/platform-settings.html` のナビゲーション `<div class="flex items-center gap-4">` 内に追加：

```html
<a href="/admin/audit-logs.html" class="text-sm text-gray-500 hover:text-gray-700">監査ログ</a>
```

既存の `<a href="/admin/platform-settings.html" ...>設定</a>` の直前に挿入する。

- [ ] **Step 3: CSSをビルドする**

```bash
npm run build:css
```

- [ ] **Step 4: コミットする**

```bash
git add admin-ui/audit-logs.html admin-ui/tenants.html admin-ui/platform-settings.html \
        admin-ui/static/tailwind.css
git commit -m "feat(audit): PF管理者向け監査ログ画面を追加"
```

---

### Task 7: テナントポータルに監査ログタブを追加

**Files:**
- Modify: `admin-ui/tenant-portal.html`

**Interfaces:**
- Consumes: `GET /tenant-portal/audit-logs?action=&from_dt=&to_dt=&limit=&offset=` (Task 3)

- [ ] **Step 1: `tabs` getter に監査ログタブを追加する**

`tenant-portal.html` の `get tabs()` 内、`{ id: 'danger', ... }` の直前に追加：

```javascript
        { id: 'audit', label: '監査ログ', icon: '📋' },
```

`isAdmin` 条件の外側（base 配列）に追加するか内側に追加するかを決める。テナント管理者（admin）のみ見えるように `isAdmin` 条件ブロック内に追加する：

```javascript
      if (this.isAdmin) {
        base.push({ id: 'public', label: '公開ダッシュボード', icon: '🌐' });
        base.push({ id: 'audit', label: '監査ログ', icon: '📋' });
        base.push({ id: 'danger', label: 'テナント削除', icon: '⚠' });
      }
```

- [ ] **Step 2: `switchTab` に audit の初期ロードを追加する**

`switchTab` 関数に追加：

```javascript
      if (tab === 'audit') await this.loadAuditLogs();
```

- [ ] **Step 3: 監査ログの state と関数を追加する**

`portalApp()` の return オブジェクト内（既存のプロパティの後）に追加：

```javascript
    // ===== 監査ログ =====
    auditLogs: [],
    auditTotal: 0,
    auditOffset: 0,
    auditLimit: 50,
    auditLoading: false,
    auditError: '',
    auditFilter: { action: '', from_dt: '', to_dt: '' },

    formatDate(iso) {
      return new Date(iso).toLocaleString('ja-JP', { timeZoneName: 'short' });
    },

    async loadAuditLogs() {
      this.auditLoading = true;
      this.auditError = '';
      try {
        const params = new URLSearchParams({ limit: this.auditLimit, offset: this.auditOffset });
        if (this.auditFilter.action)  params.set('action',  this.auditFilter.action);
        if (this.auditFilter.from_dt) params.set('from_dt', this.auditFilter.from_dt + 'T00:00:00Z');
        if (this.auditFilter.to_dt)   params.set('to_dt',   this.auditFilter.to_dt   + 'T23:59:59Z');
        const data = await portalFetch('GET', '/audit-logs?' + params.toString());
        this.auditLogs  = data.items;
        this.auditTotal = data.total;
      } catch(e) {
        this.auditError = e.message || '読み込みに失敗しました';
      } finally {
        this.auditLoading = false;
      }
    },

    async auditSearch() { this.auditOffset = 0; await this.loadAuditLogs(); },
    async auditPrev()   { this.auditOffset = Math.max(0, this.auditOffset - this.auditLimit); await this.loadAuditLogs(); },
    async auditNext()   { this.auditOffset += this.auditLimit; await this.loadAuditLogs(); },
```

- [ ] **Step 4: 監査ログタブのHTML（コンテンツ）を追加する**

`admin-ui/tenant-portal.html` の `<!-- ===== テナント削除 ===== -->` の直前に追加：

```html
          <!-- ===== 監査ログ ===== -->
          <div x-show="activeTab === 'audit'">
            <div class="flex items-center justify-between mb-4">
              <h3 class="font-medium text-gray-700">監査ログ</h3>
            </div>

            <!-- フィルター -->
            <div class="bg-white rounded border border-gray-200 p-3 mb-4">
              <div class="grid grid-cols-3 gap-3">
                <div>
                  <label class="block text-xs font-medium text-gray-600 mb-1">アクション</label>
                  <select x-model="auditFilter.action"
                          class="w-full border border-gray-300 rounded px-2 py-1.5 text-sm focus:outline-none focus:ring-1 focus:ring-amber-500">
                    <option value="">すべて</option>
                    <option value="login_success">ログイン成功</option>
                    <option value="login_failure">ログイン失敗</option>
                    <option value="logout">ログアウト</option>
                    <option value="create_device">デバイス登録</option>
                    <option value="delete_device">デバイス削除</option>
                    <option value="upload_firmware">FWアップロード</option>
                    <option value="ota_send">OTA送信</option>
                    <option value="create_tenant_user">ユーザー作成</option>
                    <option value="delete_tenant_user">ユーザー削除</option>
                  </select>
                </div>
                <div>
                  <label class="block text-xs font-medium text-gray-600 mb-1">開始日</label>
                  <input type="date" x-model="auditFilter.from_dt"
                         class="w-full border border-gray-300 rounded px-2 py-1.5 text-sm focus:outline-none focus:ring-1 focus:ring-amber-500">
                </div>
                <div>
                  <label class="block text-xs font-medium text-gray-600 mb-1">終了日</label>
                  <input type="date" x-model="auditFilter.to_dt"
                         class="w-full border border-gray-300 rounded px-2 py-1.5 text-sm focus:outline-none focus:ring-1 focus:ring-amber-500">
                </div>
              </div>
              <div class="mt-2 flex justify-end">
                <button @click="auditSearch()"
                        class="bg-amber-500 hover:bg-amber-600 text-white px-3 py-1.5 rounded text-xs font-medium">
                  検索
                </button>
              </div>
            </div>

            <div x-show="auditError" class="bg-red-50 border border-red-200 text-red-600 text-xs rounded px-3 py-2 mb-3" x-text="auditError"></div>

            <div class="bg-white rounded border border-gray-200 overflow-hidden">
              <div x-show="auditLoading" class="text-center py-6 text-gray-400 text-sm">読み込み中...</div>
              <div x-show="!auditLoading" class="overflow-x-auto">
                <table class="w-full text-sm">
                  <thead class="bg-gray-50 border-b border-gray-200">
                    <tr>
                      <th class="text-left px-3 py-2 font-medium text-gray-600 text-xs whitespace-nowrap">日時</th>
                      <th class="text-left px-3 py-2 font-medium text-gray-600 text-xs">操作者</th>
                      <th class="text-left px-3 py-2 font-medium text-gray-600 text-xs">アクション</th>
                      <th class="text-left px-3 py-2 font-medium text-gray-600 text-xs">対象</th>
                      <th class="text-left px-3 py-2 font-medium text-gray-600 text-xs">結果</th>
                      <th class="text-left px-3 py-2 font-medium text-gray-600 text-xs">IP</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-gray-100">
                    <template x-if="auditLogs.length === 0">
                      <tr>
                        <td colspan="6" class="text-center py-6 text-gray-400 text-sm">ログがありません</td>
                      </tr>
                    </template>
                    <template x-for="log in auditLogs" :key="log.id">
                      <tr class="hover:bg-gray-50">
                        <td class="px-3 py-2 whitespace-nowrap text-gray-500 text-xs" x-text="formatDate(log.created_at)"></td>
                        <td class="px-3 py-2 text-gray-700 text-xs" x-text="log.actor_email"></td>
                        <td class="px-3 py-2">
                          <span class="font-mono text-xs bg-gray-100 px-1.5 py-0.5 rounded" x-text="log.action"></span>
                        </td>
                        <td class="px-3 py-2 text-gray-500 text-xs" x-text="log.resource_id || '—'"></td>
                        <td class="px-3 py-2">
                          <span :class="log.result === 'success' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-600'"
                                class="text-xs px-1.5 py-0.5 rounded-full font-medium"
                                x-text="log.result === 'success' ? '成功' : '失敗'"></span>
                        </td>
                        <td class="px-3 py-2 text-gray-400 text-xs" x-text="log.ip_address || '—'"></td>
                      </tr>
                    </template>
                  </tbody>
                </table>
              </div>

              <!-- ページネーション -->
              <div x-show="!auditLoading && auditTotal > 0"
                   class="flex items-center justify-between px-3 py-2 border-t border-gray-200">
                <span class="text-xs text-gray-400">
                  全 <span x-text="auditTotal"></span> 件中
                  <span x-text="auditOffset + 1"></span>–<span x-text="Math.min(auditOffset + auditLimit, auditTotal)"></span> 件
                </span>
                <div class="flex gap-2">
                  <button @click="auditPrev()" :disabled="auditOffset === 0"
                          class="px-2 py-1 text-xs border border-gray-300 rounded hover:bg-gray-50 disabled:opacity-40">前へ</button>
                  <button @click="auditNext()" :disabled="auditOffset + auditLimit >= auditTotal"
                          class="px-2 py-1 text-xs border border-gray-300 rounded hover:bg-gray-50 disabled:opacity-40">次へ</button>
                </div>
              </div>
            </div>
          </div>
```

- [ ] **Step 5: CSSをビルドする**

```bash
npm run build:css
```

- [ ] **Step 6: 全テストを実行して回帰がないことを確認する**

```bash
cd core-api
pytest tests/ -v
```

Expected: 全テスト PASS

- [ ] **Step 7: コミットする**

```bash
git add admin-ui/tenant-portal.html admin-ui/static/tailwind.css
git commit -m "feat(audit): テナントポータルに監査ログタブを追加"
```
